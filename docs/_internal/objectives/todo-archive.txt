# zd md — completed tasks, moved out of docs/todo.txt by /archive
# Same format as the task list. Newest block last. Nothing here is edited or deleted.

## 2026-07-29

x 2026-07-28 (A) 2026-07-28 Archive Rust prototype to tag rust-prototype and branch archive/rust-egui +p0 @reset sess:0.1 est:15m
x 2026-07-28 (A) 2026-07-28 Move iA Writer faces and OFL from crates/zd/assets to assets/fonts +p0 @reset sess:0.1 est:5m vis:8
x 2026-07-28 (A) 2026-07-28 Prune stale docs: quickstart, dependency-justification, architecture, bdd, packaging, evidence +p0 @reset sess:0.1 est:15m
x 2026-07-28 (A) 2026-07-28 Delete crates, Cargo.toml, Cargo.lock, rust-toolchain.toml and clear target +p0 @reset sess:0.1 est:10m
x 2026-07-28 (A) 2026-07-28 Create append-only FEEDBACK.md inbox at repo root +p0 @reset sess:0.1 est:5m
x 2026-07-28 (A) 2026-07-28 Scaffold package.json tsconfig vite prettier and npm scripts +p0 @scaffold sess:0.2 est:30m
x 2026-07-28 (A) 2026-07-28 Configure eslint with max-lines 500 as a warning not an error +p0 @scaffold sess:0.2 est:15m
x 2026-07-28 (A) 2026-07-28 Scaffold src tree: suite, design, miniapps/md, platform.ts +p0 @scaffold sess:0.2 est:30m
x 2026-07-28 (A) 2026-07-28 Scaffold src-tauri skeleton with tauri.conf.json and command modules +p0 @scaffold sess:0.2 est:30m
x 2026-07-28 (A) 2026-07-28 Add miniapp registry so td can be added without touching md +p0 @scaffold sess:0.2 est:20m vis:3
x 2026-07-28 (A) 2026-07-28 Verify npm run tauri dev opens a window and vitest and playwright run +p0 @scaffold sess:0.2 est:20m
x 2026-07-28 (A) 2026-07-28 Route CLI args so zd md . and zd md file reach the frontend +p0 @scaffold sess:0.2 est:30m vis:9
x 2026-07-28 (A) 2026-07-28 Port DESIGN.md type roles to design/tokens.css custom properties +p1 @design sess:1.1 est:45m vis:8
x 2026-07-28 (A) 2026-07-28 Add @font-face for the four iA Writer faces with no synthetic weights +p1 @design sess:1.1 est:20m vis:8
x 2026-07-28 (A) 2026-07-28 Theme tokens for dark light and follow-system with warmth filter hook +p1 @design sess:1.1 est:30m vis:7
x 2026-07-28 (A) 2026-07-28 Build a type specimen page to eyeball every role in both themes +p1 @design sess:1.1 est:20m
x 2026-07-28 (A) 2026-07-28 Stop the Quattro variable axis rendering unshipped weights and bold italics +p1 @design +found found:2026-07-28 sess:1.1 est:45m vis:8 NOTDEFECT harness compared shots at different sub-pixel offsets
x 2026-07-28 (A) 2026-07-28 Wire markdown-it and transform markdown to DOM +p1 @reader sess:1.2 est:30m vis:4
x 2026-07-28 (A) 2026-07-28 Reading surface with canonical measure insets and scroll container +p1 @reader sess:1.2 est:30m vis:4.2
x 2026-07-28 (A) 2026-07-28 Render the file from the CLI arg in the reading surface +p1 @reader sess:1.2 est:20m
x 2026-07-28 (A) 2026-07-28 Heading hierarchy that is legible at a glance without shouting +p1 @reader sess:1.3 est:30m ref:F12 vis:4.2
x 2026-07-28 (A) 2026-07-28 List marker proportion indent and continuation returning to text origin +p1 @reader sess:1.3 est:45m ref:F09,F12 vis:4.2
x 2026-07-28 (A) 2026-07-28 Inline code shares the prose baseline and line rhythm +p1 @reader sess:1.3 est:20m ref:F07 vis:4.2
x 2026-07-28 (A) 2026-07-28 Inline code inside a heading keeps heading size and baseline +p1 @reader sess:1.3 est:20m ref:F10 vis:4.2
x 2026-07-28 (A) 2026-07-28 Resting states for blockquote rule table and image +p1 @reader sess:1.3 est:30m vis:4.2
x 2026-07-28 (A) 2026-07-28 Shiki syntax highlighting for fenced blocks with no visible fences +p1 @reader sess:1.4 est:45m ref:F08 vis:4.2
x 2026-07-28 (A) 2026-07-28 Focus mode dims everything outside the focus target +p1 @reader sess:1.5 est:45m ref:F04 vis:4.1
x 2026-07-28 (A) 2026-07-28 Keyboard movement updates the focus target +p1 @reader sess:1.5 est:30m ref:F04 vis:4.1
x 2026-07-28 (A) 2026-07-28 Pointer and scroll movement update the focus target +p1 @reader sess:1.5 est:30m ref:F04 vis:4.1
x 2026-07-28 (A) 2026-07-28 Incoming focus immediate and outgoing focus eased +p1 @reader sess:1.5 est:30m ref:F04 vis:4.1
x 2026-07-28 (A) 2026-07-28 Focus granularity setting for paragraph and section +p1 @reader sess:1.5 est:30m vis:4.1
x 2026-07-28 (A) 2026-07-28 CHECKPOINT read agent markdown in it for a day and file feedback +p1 @reset est:0m DEFERRED not performed skipped by request to keep building the 5 DECIDE items below still need human eyes
x 2026-07-29 (A) 2026-07-29 Dim context far harder than 65 percent the highlight is not pronounced enough +p1 @reader +fb fb:2026-07-29 est:20m vis:4.1
x 2026-07-29 (A) 2026-07-29 Stop the pointer moving focus focus is the read position not the cursor +p1 @reader +fb fb:2026-07-29 est:20m vis:4.1
x 2026-07-29 (A) 2026-07-29 Move the vertical anchor to the centre of the screen +p1 @reader +fb fb:2026-07-29 est:15m vis:4.1
x 2026-07-29 (A) 2026-07-29 Resolve a relative launch path against the invocation cwd not src-tauri +p1 @shell +fb fb:2026-07-29 est:30m vis:9
x 2026-07-29 (A) 2026-07-29 DECIDE unify reading and editing into one mode contradicts vision 4 and 6 keep or change the vision +p1 @reset +fb fb:2026-07-29 est:20m vis:6 ANSWERED yes unify fix the vision
x 2026-07-29 (A) 2026-07-29 Rewrite vision 4 and 6 and DESIGN.md 7.3 7.4 as one read edit mode always editable but readerly +p1 @reset +fb fb:2026-07-29 est:45m vis:6
x 2026-07-29 (A) 2026-07-29 DECIDE word wrap toggle in reading mode DESIGN.md 7.6 disables that row keep or change +p1 @reset +fb fb:2026-07-29 est:10m vis:6.1 ANSWERED word wrap is always toggleable there is only one mode
x 2026-07-29 (A) 2026-07-29 DECIDE re-sequence the plan so the editor comes before everything that needs a caret +p1 @reset +found found:2026-07-29 est:30m vis:6 ANSWERED editor first phase 2 is now editing phase 3 is navigation
x 2026-07-29 (B) 2026-07-29 Move reader.html and specimen.html out of the repo root +p1 @scaffold +fb fb:2026-07-29 est:20m
x 2026-07-29 (C) 2026-07-29 DECIDE whether external links get a simple browser mini app or only the system browser +p5 @shell +fb fb:2026-07-29 est:20m vis:3 ANSWERED a simple browser mini app later needs more thinking
x 2026-07-29 (A) 2026-07-28 DECIDE reading measure 620px vs canonical 560px DESIGN.md 5.3 contradicts itself needs checkpoint eyeballing +p1 @design +found found:2026-07-28 est:10m vis:4.2 ANSWERED keep 620px it reads fine and make it configurable
x 2026-07-29 (A) 2026-07-28 DECIDE whether h4 h5 h6 read as distinct levels and confirm their derived 28/18 rhythm +p1 @design +found found:2026-07-28 est:20m vis:4.2 ANSWERED they must read as distinct levels they currently do not
x 2026-07-29 (A) 2026-07-29 Make h4 h5 h6 read as distinct levels stepped space per level not bigger type +p1 @design +fb fb:2026-07-29 est:30m ref:F12 vis:4.2
x 2026-07-29 (A) 2026-07-28 DECIDE the vertical anchor position currently a third down and unspecified in DESIGN.md 7.6 +p1 @reader +found found:2026-07-28 est:15m vis:4.1 ANSWERED by feedback centre of the screen
x 2026-07-29 (A) 2026-07-28 Stop the focus target jumping one block on first scroll open focus and anchor disagree +p1 @reader +found found:2026-07-28 est:30m vis:4.1 WAS three blocks not one fixed with the leading focus gutter
x 2026-07-29 (B) 2026-07-29 DECIDE whether to lower the focus context floor below 3 to 1 only if 90 percent dim is still not enough +p1 @reader +found found:2026-07-29 est:15m vis:4.1 ANSWERED 90 percent is enough the floor stays at 3 to 1
x 2026-07-29 (B) 2026-07-28 CodeMirror 6 editing surface in our own type and colour never its default theme +p2 @editor sess:3.1 est:45m vis:6.3

## 2026-07-29

x 2026-07-29 (A) 2026-07-29 Heading lines take their reading typography and rhythm while the hash stays on screen +p2 @editor sess:3.2 est:45m vis:6.1
x 2026-07-29 (A) 2026-07-29 Quote and fence lines take their reading geometry with literal markers +p2 @editor sess:3.2 est:45m vis:6.1
x 2026-07-29 (A) 2026-07-29 List lines take their reading geometry the marker must be given the column width not assumed to already have it +p2 @editor sess:3.2 est:45m ref:F12 vis:6.1 SPLIT from the quote and fence task do with the nesting task below
x 2026-07-29 (A) 2026-07-29 Focus follows the caret in the unified mode and scrolling for context does not move it +p2 @editor sess:3.2 +fb fb:2026-07-29 est:45m vis:4.1
x 2026-07-29 (A) 2026-07-28 Line granularity painting real shaped rows never a character estimate +p2 @editor sess:3.2 est:45m ref:F04 vis:4.1
x 2026-07-29 (B) 2026-07-28 Atomic cmd+s save through the platform boundary +p2 @editor sess:3.1 est:30m vis:6.3 the editor asks and the caller writes wiring it to a real document lands with the flip below
x 2026-07-29 (B) 2026-07-28 Track unsaved state so the document knows whether it has been written +p2 @editor sess:3.1 est:20m vis:6.3 SPLIT from the visible unsaved state task
x 2026-07-29 (A) 2026-07-30 Make the document surface the editor +p2 @editor sess:3.2 est:60m vis:6.1 SPLIT retiring the render path is below

## 2026-07-29

x 2026-07-29 (A) 2026-07-29 Show markdown source markers in the gutter hashes outside the left column dash lists +p2 @editor +fb fb:2026-07-29 est:45m ref:F05 vis:6.1
x 2026-07-29 (B) 2026-07-28 Markdown source markers nesting and continuations share fixed geometry +p2 @editor sess:3.2 est:45m ref:F05 vis:6.1
x 2026-07-29 (B) 2026-07-29 Blockquote marker hangs outside the quote text column DECIDE the notation gutter beside a heading hash or just outside its own hairline +p2 @editor +found found:2026-07-29 est:30m ref:F05 vis:6.1 SEEN 7.4 names hash quote and dash the gutter task did the hash only ANSWERED its own indent inside the hairline the gutter is for markers whose block owns no indent
x 2026-07-29 (B) 2026-07-28 Focus modes work in the markdown editor the same as in reading +p2 @editor sess:3.2 est:30m vis:6.1
x 2026-07-29 (B) 2026-07-29 Show unsaved state somewhere DESIGN.md 7.4 forbids a persistent dirty indicator in the document so it needs the 7.10 status strip or the 6.2 dirty close prompt and Tauri 2.11 has no set_document_edited +p2 @editor sess:3.1 est:30m vis:6.3 SPLIT the state is tracked already
x 2026-07-29 (A) 2026-07-29 DECIDE vision 6.1 says notation is always visible the inbox asks for rendered by default with a raw toggle rewrite it or refuse +p2 @reset +fb fb:2026-07-29 est:20m vis:6.1 ANSWERED short notation stays literal hashes list markers quote marks single backticks emphasis everything else renders raw mode toggle off by default

## 2026-07-30

x 2026-07-29 (A) 2026-07-29 Dim unfocused text to barely legible contrast floors do not apply here +p1 @reader +fb fb:2026-07-29 est:20m vis:4.1 REVERSES the answered 3 to 1 floor decision in todo-archive.txt REOPENED 2026-07-29 the 90 percent override still is not enough separation and this has been raised repeatedly
x 2026-07-29 (A) 2026-07-29 A caret in a list focuses the whole list the editor stopped at the ListItem while the reader always used the whole ul +p2 @editor +fb fb:2026-07-29 est:20m vis:4.1 ANSWERED DESIGN.md 7.6 said block/list item which permitted both it now names the list
x 2026-07-29 (A) 2026-07-28 Single shortcut registry where every binding resolves to a handler +p4 @shell sess:4.2 est:45m ref:F16 vis:7.1 PULLED FORWARD 2026-07-29 the Reference below renders this registry so it cannot be a hand-maintained list
x 2026-07-29 (A) 2026-07-28 Shortcut Reference on cmd+. opens and closes preserving context +p4 @shell sess:4.3 est:30m ref:F02 vis:7.1 PULLED FORWARD 2026-07-29 by request asap so it is possible to see what commands exist
x 2026-07-29 (A) 2026-07-28 Unavailable commands presented honestly rather than as working shortcuts +p4 @shell sess:4.3 est:20m ref:F16 vis:7.1 PULLED FORWARD 2026-07-29 this is the half of cmd+. that answers whether a command actually works
x 2026-07-29 (A) 2026-07-29 Every source marker takes the type role of the construct it marks a heading hash the heading role a list dash and quote mark the prose role inline backticks the code role +p2 @editor +fb fb:2026-07-29 est:20m vis:6.1 ANSWERED 2026-07-29 DESIGN.md 5.2 rewritten the old always-inline-code rule is gone face and size follow the construct colour stays text.muted and an unmatched backtick is prose not a marker SEEN only .md-notation-mark sets a face today so the fix is mostly deleting two declarations
x 2026-07-29 (A) 2026-07-29 Heading hashes drift further out of alignment down the document until one sits above the heading it belongs to +p2 @editor +fb fb:2026-07-29 est:45m vis:6.1 FIXED EARLIER by the white-space nowrap in the marker role commit the marker box wrapped inside itself so a hash grew taller the more hashes a heading had which is why the error compounded down the document VERIFIED zero drift at every level at five scroll positions at a 700px viewport where CodeMirror actually virtualizes REGRESSION TEST added and proved by removing nowrap and watching it fail on the h3
x 2026-07-29 (A) 2026-07-29 Links read as links in the editor not as raw bracket and paren markdown +p2 @editor +fb fb:2026-07-29 est:30m vis:6.1
x 2026-07-29 (A) 2026-07-29 Tables read as tables in the editor not as raw pipe markdown plain cell text first +p2 @editor +fb fb:2026-07-29 est:60m vis:4.2 SPLIT 2026-07-29 a real table means a block widget replacing a multi line node and CodeMirror throws Block decorations may not be specified via plugins so it needs a StateField too which is more than one sitting with the widget and the CSS
x 2026-07-29 (A) 2026-07-29 Inline markup inside table cells renders rather than showing literal source a link or backticks in a cell +p2 @editor +fb fb:2026-07-29 est:45m vis:4.2 SPLIT from the table task above which lands plain cell text
x 2026-07-29 (A) 2026-07-29 Fenced code takes the code plane and language highlighting in the editor +p2 @editor +fb fb:2026-07-29 est:45m ref:F08 vis:4.2 ADDED @codemirror/lang-rust and mapped lezer tags to the reader's own md-syn classes so neither highlighter knows what a colour is PLANE was already correct only the highlighting half was missing
x 2026-07-29 (A) 2026-07-29 Single backtick inline code takes the code style in the editor +p2 @editor +fb fb:2026-07-29 est:20m ref:F07 vis:4.2
x 2026-07-29 (A) 2026-07-29 Fence backticks and the language tag stop showing once the block is formed +p2 @editor +fb fb:2026-07-29 est:30m vis:6.1 SEEN editor-blocks.spec.ts 185 asserts the fence rows are on screen citing 6.1 that assertion inverts with this task
x 2026-07-29 (A) 2026-07-29 Raw mode shortcut reveals the literal markdown the rendered default hides +p2 @editor +fb fb:2026-07-29 est:45m vis:6.1 BLOCKS NOTHING FORMALLY BUT the caret cannot enter a rendered table or a link destination or a hidden fence line so raw mode is currently the only possible way to reach that source at all which argues for taking it before the remaining render tasks DONE cmd+e provisional chord neither the vision nor DESIGN.md names one flag lives in editor state so decorations recompute in the same transaction and two windows can differ
x 2026-07-29 (B) 2026-07-29 Images render in the editor instead of showing bracket and destination source +p2 @editor +found found:2026-07-29 est:20m vis:6.1 one of the four blockers on retiring the render-only path DONE through the reader's renderInlineMarkdown so the never-fetch-a-remote-image guarantee comes with it a test asserts no request to example.com is made
x 2026-07-29 (B) 2026-07-29 Horizontal rules draw as rules in the editor instead of literal dashes +p2 @editor +found found:2026-07-29 est:20m vis:6.1 one of the four blockers on retiring the render-only path
x 2026-07-29 (A) 2026-07-29 Links render in the reader too reading.css has no a rule and the fixture has no link at all so links now work in the editor and are unbuilt in the reference +p2 @reader +found found:2026-07-29 est:30m vis:4.3 SEEN text-link sat in tokens.css consumed by nothing until the editor task DESIGN.md 7.3 asks for the same there hide source markers links show their labels not destinations SAME CLASS as the list focus divergence and the reader is what the editor specs measure against so a construct it lacks is a construct nothing compares DONE one rule in md.css over both selectors and a test comparing the two surfaces on a target block
x 2026-07-29 (B) 2026-07-29 Autolink angle brackets stop showing the address is the label so nothing hides but the angles are notation +p2 @editor +found found:2026-07-29 est:20m vis:6.1 SEEN lezer parses this as its own construct not a Link so the link work missed it DONE Autolink has the same LinkMark URL LinkMark shape but the URL is the label so only the delimiters go the reader already handled it and now has one in its fixture to compare against
x 2026-07-29 (B) 2026-07-29 Setext headings take their reading typography the way ATX headings do +p2 @editor +found found:2026-07-29 est:20m vis:6.1 DECIDED the underline row is not drawn either on the same reasoning 5.2 gives for the hash notation the size of prose beside a 30px heading reads as debris and an underline is a whole row of it raw mode reveals it fence.ts became notation-rows.ts since a fence marker and a setext underline are the same subject a line whose entire content is notation
x 2026-07-29 (B) 2026-07-29 Indented four space code blocks take the code plane the way fenced ones do +p2 @editor +found found:2026-07-29 est:20m vis:6.1 DECIDED the four space marker is hidden per row exactly four so relative indentation survives because markdown-it strips it too and two ways of writing one construct must not sit at two different origins
x 2026-07-30 (A) 2026-07-29 The focus anchor reads too low ANCHOR_RATIO in reader/focus.ts is 1/2 exact geometric centre it was 1/3 and was moved on a literal reading of the word center put it back where the eye actually rests +p2 @reader +fb fb:2026-07-29 est:30m vis:4.1 NOT A VISION CONFLICT the spec means where you are reading and 4.1 wording follows whatever ratio lands DONE vision 4.1 and DESIGN.md 7.6 now name the position so a literal reading cannot move it a third time FOUND the ratio had five hand-written copies outside the constant the symmetrical 50vh gutter two innerHeight/2 inset measurements and two scrollIntoView block center scrolls every one of which agreed with itself and not with the product
x 2026-07-30 (A) 2026-07-29 A document opens with its first line higher on screen +p2 @editor +fb fb:2026-07-29 est:20m vis:4.1 SAME CAUSE as the anchor task above the first block is placed on the anchor so at 1/2 it opens halfway down the window fix the ratio first then recheck this FIXED BY the same commit the first line now opens 31 percent down the window measured instead of 48 percent initFocus also stopped using scrollIntoView block center which is a second name for the midpoint and would have left the opening block half a screen off the anchor
x 2026-07-30 (A) 2026-07-30 Arrow keys move the caret by what is on screen not by what the source contains +p2 @editor +fb fb:2026-07-30 est:60m vis:6.1 SPLIT 2026-07-30 out of the caret umbrella task below which did not land in one sitting CONFIRMED the suspicion nothing was registered in CodeMirror's atomicRanges facet while four kinds of replace decoration hid text so crossing a rendered link cost one press per character of a URL nobody can see DONE all four sources are atomic now notation.ts returns hidden ranges as a second set so marks and line decorations stay out of it deliberately since an atomic inline-code span would skip visible editable text TESTED as the longest run of presses that leave the caret at the same place on screen which is the complaint itself and is 1 now and was 27
x 2026-07-30 (A) 2026-07-30 Clicking a line puts the caret on the line below it +p2 @editor +fb fb:2026-07-30 est:45m vis:6.1 SPLIT 2026-07-30 from the caret umbrella task the atomic ranges half was done first CORRECTION the earlier claim that this starts at the top of the document was a measurement artifact the probe dispatched clicks in a loop and read rects between them while focus and layout moved REMEASURED as a round trip position to coordinates to position which needs no text matching and no dom line indexing and every mismatch sits after the table lines 92 onward while every fence line above it is exact ROOT CAUSE the editor table carried margin-block and CodeMirror builds its height map from getBoundingClientRect which excludes margin so the table occupied 294px against a map that held 246 and 48px is nearly two rows FIXED by spending the same gap-block as padding on the editor half only the reader keeps margin because 5.3 needs collapsing GUARD a test asserts a block widget's occupied space equals its own box so the next widget inherits the rule not the bug
x 2026-07-30 (A) 2026-07-30 Up arrow from below the table no longer crosses it but vertical motion near it is still wrong +p2 @editor +fb fb:2026-07-30 est:30m vis:6.1 FIXED BY the table padding commit the atomic range stopped the caret entering the widget and the height map fix stopped every position after it resolving to the wrong row VERIFIED every non blank line in the fixture now round trips
x 2026-07-30 (A) 2026-07-30 The paragraph beside the table cannot be selected or stepped through and reads as covered by something +p2 @editor +fb fb:2026-07-30 est:30m vis:6.1 FIXED BY the table padding commit covered by something was literal in effect the height map put every position after the table one to two rows above where it was painted so a click landed in the neighbouring line and a selection started somewhere other than where it looked
x 2026-07-30 (A) 2026-07-30 Releasing cmd+. closes the Shortcut Reference which frees Escape for dropping the caret +p4 @shell +fb fb:2026-07-30 est:20m vis:7.1 ANSWERS the Escape DECIDE below no chord is shared the Reference is held rather than toggled DONE the registry gained held commands a Command may declare release and keyup ends the hold whichever key of the chord comes up first because nobody releases two keys at once WHY IN THE REGISTRY a keyup listener in reference.ts would be half a binding the registry cannot describe which is F16 starting again transient.dismiss is deleted not merely unbound since a held sheet cannot still be there when a separate key could be pressed and 7.1 forbids listing a binding that cannot run CONTRADICTED vision 7.1 pressing it again which is now recorded as hold-to-view with the reason and DESIGN.md 8 now says a held surface is not a transient SPEC NOTE the Reference is a glance not a mode so nothing in it is interactive and a hold prevents nothing
x 2026-07-30 (A) 2026-07-30 The editor's focus anchor lands on a blank line between blocks and so focuses nothing +p2 @editor +found found:2026-07-30 est:30m vis:4.1 CORRECTION the filed claim said the anchor stops tracking once scrolled which is FALSE it tracks correctly at 400 the target is the paragraph containing the anchor line and at 1100 it is the whole list I read truncated probe output saw two empty strings and called it stuck without checking which line each was REAL DEFECT was narrower the anchor is one y coordinate so it often lands in the blank line between two blocks and a markdown source has real blank lines where the reader's DOM has only margin so focusing one focuses nothing on a surface whose job is to dim everything else THE READER always snapped its blockAtY says the block the anchor sits in or the nearest one when it lands in a gap so this was one rule answered differently on two surfaces the same class as the list focus divergence FIXED nearestContentPos snaps the anchor path only the caret still gets a blank line as its own target because it has intent behind it and a test pins that half too TEST TRAP the first assertion went vacuous twice first on empty text which a horizontal rule legitimately has and then on children.length because CodeMirror puts a br in every empty line
x 2026-07-30 (A) 2026-07-29 Blockquote continues on enter and exits on double enter +p2 @editor +fb fb:2026-07-29 est:45m vis:6.1 SEEN lang-markdown ships insertNewlineContinueMarkup and deleteMarkupBackward held off behind addKeymap false DEFERRED THREE TIMES 2026-07-30 by caret and focus defects taking priority SPLIT 2026-07-30 the fence half is its own task below the library does blockquotes and lists and explicitly not fences FOUND the library does not leave a blockquote either its docs say enter removes trailing whitespace and LIST markers and a quote marker is neither so a second enter gave another quote line forever which is the reported complaint with the continuation working DONE one command of ours leaveBlockquote demotes a line holding only quote markers and declines in every other case so it layers ahead of the library without taking Enter from it WHY A KEYMAP AND NOT THE REGISTRY Enter has no chord no description and no Reference row so it is text editing which is why defaultKeymap has always been here
x 2026-07-30 (A) 2026-07-30 A code fence opens a block on enter and a second enter leaves it +p2 @editor +fb fb:2026-07-29 est:45m vis:6.1 SPLIT 2026-07-30 out of the blockquote task above which covered the two constructs lang-markdown handles CUSTOM WORK insertNewlineContinueMarkup does blockquotes and lists only so both halves of this are ours DONE closeFence and leaveFence in continuation.ts each claiming one case and declining otherwise DETECTION off CodeMark children the way notation-rows.ts already does it one mark means unclosed which is a fact that file had already established and verified CLOSER repeats the opener's own characters at its own length since a fence may be tildes or longer than three and CommonMark needs the closer to match LEAVE takes the scaffolding blank line back and only fires from the line directly above the closer because a blank line elsewhere is someone spacing out code BUG FOUND resolveInner with side minus one at a line start resolves to the line before which is outside the fence so both commands declined on every fence in the document until it was a plus one
x 2026-07-30 (A) 2026-07-30 Typing a fence pushes the caret off the fence line so a language cannot be typed at all +p2 @editor +found found:2026-07-30 est:30m vis:6.1 A NOT B because it makes the fence continuation shipped 2026-07-30 unreachable by typing which is the only way a person creates a fence MEASURED typing three backticks then rust gives two source lines the backticks and then rust on the line below the caret leaves the fence line between characters PREDATES the continuation commands it reproduces with them removed SUSPECT the atomicRanges work an incomplete fence keeps its row on purpose but something treats the position at its end as unreachable CHECK inline code and links the same way since they hide source too FOUND IT BY READING not by bisecting notation-rows.ts hid the opening row of a fence one statement BEFORE the marks.length check that exists to prevent exactly that and the comment above the check described the bug the code had CONSEQUENCE a hidden row is also an atomic range so the caret was pushed off the line the moment the third backtick landed FIXED by moving the guard above the push CHECKED inline code and links typed one character at a time both were already safe and now have regression guards PROVED by moving the guard back and watching both new specs go red NOTE insertText hides this bug completely because the construct never exists half typed so the specs that reproduce it deliberately type character by character
x 2026-07-31 (A) 2026-07-31 Rendered tables do not render the markdown inside their cells +p2 @editor +fb fb:2026-07-31 est:30m vis:4.2 CONTRADICTS a task marked done 2026-07-29 inline markup inside table cells renders rather than showing literal source so this is either a regression or that completion was false and finding out which comes first EVIDENCE the fixture has a cell holding backticked inline code and a cell holding a link and editor-tables.spec.ts asserts on both so if the report is right the specs are passing on something that is not what the user sees ANSWER neither a regression nor a false completion BOTH REPORTS WERE TRUE the structure was right all along the backticks were gone and a real code element was there which is what the 2026-07-29 specs asserted and nothing styled it so a code span in a cell rendered as ordinary prose IGNORED is the accurate word ROOT CAUSE the reader styles real code and a elements and the editor styles CodeMirror mark classes md-inline-code and md-link-label while the table widget renders READER dom inside the EDITOR so it matched neither selector list AND reading.css is never loaded on the editor page which md.css already says in its own comments FIXED a md-rendered class on the widget and the inline code rule moved out of reading.css into md.css where both surfaces reach it the same move already made for tables syntax categories links and images LESSON the old specs were not wrong they were incomplete they asserted the element exists and never asked whether it looks like anything AN ELEMENT IS NOT A RENDERING
x 2026-07-31 (B) 2026-07-31 The status strip fading forces the document back to the top +p2 @shell +fb fb:2026-07-31 est:30m vis:7.1 SEVERITY higher than a B in my reading since it loses your place in a long document but it arrived unmarked and the convention is that a leading bang decides SUSPECT WAS WRONG the strip is innocent and so is the fade MEASURED four ways appending the strip alone moves nothing pressing a key with no command behind it moves nothing doing neither moves nothing and cmd+i moves the surface from 1283 to 409 which is where the caret is REAL CAUSE the document moves the instant the chord is pressed and it moves to the caret so after reading ahead it throws you back the fade was a coincidence of what was on screen at the time ROOT the registry listened in the BUBBLE phase on window so CodeMirror's own handler on cm-content ran first and scrolled its selection into view before preventDefault could be reached 4.1 forbids exactly that scrolling for context leaves focus where it is FIXED attachShortcuts listens in the capture phase so a claimed chord never reaches the editor as an editing key COST capture sees every key first so dispatch claiming only what it has a command for stops being a nicety and a spec pins that typing still reaches the editor EXPOSED my own word wrap test from 2026-07-31 asserted scrollTop equals 600 and passed ONLY because of this bug CodeMirror's scroll-into-view landed near 600 by coincidence unwrapping legitimately changes scrollTop since every line gets shorter so the assertion now measures where the caret's line sits on screen which is the actual claim NO CLOCK NEEDED the jump is at the keypress so the spec installs none
x 2026-07-31 (B) 2026-07-30 Clicking the last line of a code block places the caret below the block instead of on that line +p2 @editor +fb fb:2026-07-30 est:20m vis:6.1 RECHECK FIRST 2026-07-30 the atomic ranges and the table height map fix both landed and the round trip test now finds no mismatch anywhere in the fixture including inside fences so this may already be gone NOTE the fixture's fences may not reproduce it look for a fence whose last line is the last thing before a block boundary RECHECKED 2026-07-31 IT NO LONGER REPRODUCES measured at both ends of every fence's last code line at the far right of all 115 drawn lines and against four boundary shapes the fixture lacks a fence at the end one before a heading one before a list and one last in the file WHICH CHANGE TOOK IT IS NOT KNOWN the obvious candidate was the fence row guard from 2026-07-30 since a hidden row is an atomic range and a closing fence's hidden range begins at the end of the last code line so I reintroduced that bug and the new specs still passed which means the attribution would have been a guess and is left unmade AND it means the new specs are NOT a regression test for this report they are coverage of a position class nothing else reached the far right of a line where the round trip spec cannot look because it clicks where the caret was already drawn NO PRODUCT CODE CHANGED
x 2026-08-01 (A) 2026-07-31 The e2e suite is failing under its own parallelism +p4 @editor +found found:2026-07-31 est:45m vis:12 A NOT B because a suite that fails at random stops being evidence and every session from here reads it as amber without knowing what amber means MEASURED yesterday the full run was 16 seconds today it was 12.8 minutes and after cutting one sweep spec to a stride it is 1.6 minutes with four unrelated specs red that pass in 2.5 seconds when run together CULPRIT CLASS sweeping specs that touch every line or every scroll position editor-anchor is 6.3s alone editor-caret 3.6s focus-dim 3.9s and seven of those in parallel starve each other FIRST STEP decide whether the answer is fewer workers a stride in each sweep or splitting the slow specs into their own project MORE DATA 2026-07-31 running only the editor specs at three workers still took 1.9 minutes and still failed three that pass in isolation so worker count alone is not the answer RULE TO ADOPT a spec that iterates the whole document has to justify iterating the whole document because wall time in a parallel run is not paid by the spec that spends it a stride of seven covered the same constructs as all 115 lines NOTE this is also almost certainly what the three tracked flakes have been all along so it may close that task too CLOSED 2026-08-01 NOT BY A PARALLELISM CHANGE AT ALL WHICH MAKES THE TITLE OF THIS TASK WRONG the workers are untouched and the answer to its FIRST STEP is none of the three options it lists MEASURED FOUR CONSECUTIVE FULL RUNS 326 passed at 15.6 15.4 15.3 and 15.5 seconds which is the 16 seconds of the day before the degradation ROOT CAUSE was one spec file editor-focus-line.spec.ts where four tests clicked a line and read the painted row in the same breath the row is MEASURED through requestMeasure and a microtask so it lands the frame AFTER the click by design and until then the painted row is whatever it was before which on a fresh page is the H1 at position 0 SO THE FAILURE PRESENTED AS focus does not follow the caret and it presented intermittently because whether the frame had run depended on machine load which is what made it look like a parallelism problem PLUS the file never waited for the prose face so Playwright computed a click box from the fallback font and the swap moved the line out from under the click FIXED with a clickAndSettle helper that polls until the painted row is in the clicked line and a font wait in beforeEach 75 of 75 repeats green then the whole focus group green at three workers in 4.6 seconds FOUND WHILE verifying an unrelated session which is exactly the cost this task was filed about I had reported this family as a tracked flake that passes in isolation for two sessions running and it does not it failed 4 of 4 in isolation and predates the block jump commit A FLAKE AND A RED TEST ARE NOT THE SAME CLAIM and calling one the other is how a suite stops being evidence THE STRIDE from 2026-07-31 stays and its rule is carried out into its own task below since the rule is still right and is no longer what this task is about
x 2026-08-01 (B) 2026-07-30 cmd+left and cmd+right are buggy +p2 @editor +fb fb:2026-07-30 est:20m vis:6.1 PROBABLY the same root line start and line end have to skip hidden notation rather than land inside it RESTORED 2026-07-31 this line was clobbered by a careless string replace that inserted the suite task over its subject and left its tags trailing SUSPECT WAS WRONG line start and line end do not land inside hidden notation MEASURED the keys reach the right places and do not STAY there three presses of cmd+left on a heading gave 2 then 0 then 2 and on an indented code block 3477 then 3473 then 3477 so pressing twice returns the caret to where it started which is the whole report TWO THINGS THAT LOOK WRONG AND ARE NOT cmd+right stopping mid blockquote is correct because that line wraps and line boundary motion is per visual row and cmd+left stopping after a heading's hash rather than before it is defensible since the reading column's left edge is the text edge CAUSE is layout not markdown a heading's hash is drawn left of the content origin by a negative margin and an indented block's four spaces are replaced so the start of this visual line resolves to two different offsets depending which side of the notation you ask from a list item is stable because its marker is part of its text FIXED motion.ts wraps the library's four line boundary commands in one invariant a backward command may never move the caret forward MY FIRST ATTEMPT DECLINED and did nothing because defaultKeymap binds the same chords so returning false handed the press to the binding whose behaviour is the bug A GUARD THAT LETS THE THING IT GUARDS AGAINST RUN NEXT IS NOT A GUARD mac only since Ctrl+arrow is word motion elsewhere and claiming it would change a key on two platforms to fix a bug neither has
x 2026-07-31 (B) 2026-07-31 With word wrap off text runs off the side and cannot be scrolled to +p2 @editor +fb fb:2026-07-31 est:30m ref:F03 vis:6.1 MAKES the wrap toggle shipped 2026-07-31 worse than useless in one direction you can turn wrapping off and then cannot read what you unwrapped SUSPECT md.css sets overflow-x hidden on md-surface which was right while every line fitted the measure TENSION 7.3 forbids drawing an application scrollbar track thumb or indicator so this has to scroll without one which is what the OS overlay is for SEE the same rule already allows vertical scrolling with scrollbar-width none LESSON overflow-x hidden still scrolls from script so a measurement saying the content is there proves nothing ask whether a READER can reach it CONFIRMED the suspect was right for once md-surface had overflow-x hidden MEASURED at a 520px width unwrapped the surface had scrollWidth 746 against clientWidth 520 so the content was there and overflowing and setting scrollLeft from script moved it 226px which is the tell hidden still scrolls programmatically and denies it to the person only FIXED overflow-x auto WHY THE SURFACE and not the column or CodeMirror's own scroller a heading's hash hangs outside the column on a negative margin so a scroll container there would clip every marker at its left edge the surface is wider than the column by the 5.3 inset which is the space those markers hang in GUARDS two more specs one that a wrapped document still cannot drift sideways since auto scrolls only what overflows and one that 7.3's no-scrollbar rule holds on the new axis too
x 2026-08-01 (B) 2026-07-30 The window does not scroll to follow the caret past the last visible line +p2 @editor +fb fb:2026-07-30 est:30m vis:6.1 SEEN the caret leaves the window and nothing scrolls until it reaches the table which suggests the widget is what triggers a measure SUSPECT WAS WRONG AGAIN the widget has nothing to do with it MEASURED by walking the caret down one line at a time it leaves the screen at the 19th press hundreds of lines before any table ROOT CAUSE the scroll container was taller than the window md-surface is height 100 percent inside a 700px chain but box-sizing border-box cannot produce a box smaller than its own padding and its padding was the two focus gutters plus the 5.3 insets 318px above and 589px below at that window so 907 in total THE SURFACE WAS 907 TALL IN A 700 WINDOW with 207px of every document permanently below the fold and 7.3 forbids the window scrolling to reach it CODEMIRROR WAS CORRECT THROUGHOUT it scrolls the caret into view and view means its scrollable ancestor so the caret was inside the surface and not on screen at the same time FIXED the gutters and insets moved from the container's padding to the column's margin margin on the content is scroll extent exactly as padding on the container was which is what 5.3 asks for and it cannot inflate the container PREDATES the asymmetric gutters the symmetric 50vh version was 900 at the same window so this was true from the day the gutters were added GUARD a spec asserts the surface is never taller than its window which is the root cause stated where it can be caught rather than only where it hurts
x 2026-08-01 (B) 2026-08-01 Option and arrow keys jump the caret to the next focus block not the editor's own idea of a jump +p2 @editor +fb fb:2026-08-01 est:30m vis:4.1 THE POINT is that the jump uses the same definition of a block that focus does 4.1's target rather than CodeMirror's paragraph or group motion SEEN option+arrow already jumps but by the editor's rule USER'S REASONING option sits next to the arrow keys on a mac keyboard so it is a comfortable chord NOTE focus granularity is selectable so at line granularity this is the same as a plain arrow which is worth saying out loud rather than special casing MEASURED FIRST option+down moved one SOURCE line at a time 8 then 9 then 10 then 11 from a start on line 7 so it stopped twice inside a three line paragraph that focus treats as one block DONE jumpBlock in motion.ts asks blockRange and sectionRange the functions focus itself uses and granularityOf is now exported for that so there is no second opinion about what a block is WALKS OUT of the current block then past the blank lines between blocks because those are gaps not blocks which is the same distinction nearestContentPos makes for the anchor LANDS at the block's first position rather than keeping a column so a second press is predictable CLAIMS THE KEY at the document ends per what the line boundary wrapper taught EVERY PLATFORM unlike the Cmd chords there is no existing meaning worth preserving and arrows are safe from the macOS compose problem since Option+ArrowDown has no printable form
x 2026-08-01 (B) 2026-07-30 A paragraph and the code block immediately below it may want to take focus together +p2 @reader +fb fb:2026-07-30 est:30m vis:4.1 SEEN reported on the Needs Node and Rust line focusing without the fence under it LOOK FIRST this may be paragraph granularity working exactly as 7.6 specifies rather than a defect and section granularity may already answer it LOOKED AND BOTH HALVES OF THAT NOTE HELD NOT A DEFECT 7.6 says paragraph means one semantic block and a fenced code block is a different semantic block from the paragraph that introduces it so focusing the prose alone is the specification MEASURED on both surfaces at the fixture's own instance of the reported shape paragraph granularity takes the paragraph and leaves the fence as context section granularity takes the owning heading the paragraph the fence and the rest of the section THE TWO SURFACES AGREE EXACTLY which is worth knowing because one construct described twice and drifting is this codebase's recurring defect and this is the most load-bearing question either surface answers NO PRODUCT CODE CHANGED and the new specs are therefore COVERAGE NOT REGRESSION TESTS stated in the file WHY WRITE THEM every focus spec so far measured how wide a target is and none measured what falls just outside one which is why a one-line report cost a two-surface measurement WHAT IS ACTUALLY MISSING is not in the focus code it is that granularity has no control surface so section is unreachable outside the dev fixture hook see the settings task and the want itself is a DECIDE opened below
x 2026-07-31 (A) 2026-07-29 Make word wrap a live toggle DESIGN.md 7.6 row already dropped +p2 @editor sess:3.5 +fb fb:2026-07-29 est:30m ref:F03 vis:6.1 DEFERRED TWICE 2026-07-30 by editor defects that made shipped work unreachable DONE a Compartment holds EditorView.lineWrapping so reconfiguring keeps the caret the history and the scroll position which recreating the view would all throw away and a test pins all three because rebuilding was the shorter change that would have passed the reflow tests CHORD Mod-Alt-z looked up not invented the first prototype used it and so does VS Code FOUND F03's own coverage note names a first prototype regression command_option_z_consumes_the_composed_text_event which is the macOS compose problem so EVERY alt chord in the registry was broken matches compared event.key and Option+Z delivers key omega with code KeyZ FIXED alt chords now match the physical key and nothing else does since without Alt the character is the honest thing to match so Mod-s still works on a non US layout NOT PROVABLE IN PLAYWRIGHT which synthesises key z for Alt+z so the composed case is a unit test with key omega
x 2026-07-30 (B) 2026-07-29 Enter on the last item of a list continues the list +p2 @editor +fb fb:2026-07-29 est:20m vis:6.1 SEE the blockquote and fence task the same lang-markdown command covers lists DONE BY that task as predicted a nested item continues at its own depth too and a second enter demotes which the library already did for list markers specifically two specs cover both
x 2026-08-01 (B) 2026-07-29 Tab and shift tab indent and outdent list items including a multi item selection +p2 @editor +fb fb:2026-07-29 est:45m vis:6.1 DONE lists.ts with one command shape used twice moveItems in and moveItems out THE UNIT IS THE PREVIOUS SIBLING'S CONTENT COLUMN not a fixed number of spaces because CommonMark nests an item under the one above only when it reaches that item's content column which is 2 after a dash 3 after 9 dot and 4 after 10 dot SO A FIXED TWO SPACE UNIT under a 9 dot marker makes a second list rather than a nested one which is markdown that parses renders wrongly and reads exactly like the feature working THIS IS WHY indentMore from codemirror commands is not used it is line based and unit based and would do precisely that THE SUBJECT IS AN ITEM NOT A LINE a ListItem node already spans its continuation lines and any list nested inside it so shifting the item shifts all of them and it stays one item indenting only the marker's line would strand the continuation at the old column where it stops being part of the item MULTI ITEM works by dropping any selected item that sits inside another selected item since a parent's range already covers its children so shifting both would move the children twice and flatten the nesting DECLINES IN PROSE deliberately CodeMirror leaves Tab unbound so the keyboard can leave a text surface DESIGN.md 9 claims keyboard-only editing and Escape drops the caret without giving up focus so Tab is the way out and claiming it document-wide to serve lists would trap the keyboard everywhere else CLAIMS IT INSIDE A LIST EVEN WHEN NOTHING MOVES the first item has nothing to nest under and an outermost item has nowhere to go so the answer is a key that does nothing rather than focus leaping out of the document mid edit TEST TRAP WORTH THE NAME the e2e multi item case PASSED with the ancestor dropping filter deleted because Shift+ArrowDown walks WRAPPED VISUAL ROWS and the selection did not end where the line arithmetic suggested PROVED by deleting the filter and watching e2e stay green while the unit case went red RULE a claim about a selection spanning items belongs where the selection can be stated as offsets and the browser test is left to prove only what needs a browser which here is that the keys are bound at all
x 2026-07-30 (A) 2026-07-30 Esc drops the caret and hands focus back to the anchor block +p2 @editor +fb fb:2026-07-29 est:20m vis:4.1 DONE a dropCaret StateEffect clears the sticky caretPlaced flag inside a transaction so decorations recompute in the same update and the effect is checked AFTER the selectionSet branch because dropping the caret sets the selection so otherwise Escape sets the flag it just cleared NOT A BLUR caretPlaced survives blur on purpose so contentDOM.blur alone would look right for one frame and leave the surface deaf to scrolling forever WAS HALF DELIVERED the hands-focus-back half could land on a blank line COMPLETED 2026-07-30 by the anchor snapping task above so Escape now hands focus to a real block wherever it is pressed STILL OPEN reported three times UNBLOCKED 2026-07-30 the decision came back as neither option "esc is set to close shortcut menu when releasing cmd+. should just close it esc should be set to unfocus the editor" so Escape belongs to the editor alone and the Reference stops owning it PROMOTED to A by that feedback UNBLOCKED FULLY 2026-07-30 the cmd+. release task landed Escape is registered to nothing now and a spec asserts that so this task can just claim it
x 2026-08-01 (B) 2026-07-29 Focus does not bind the whole block a sentence late in a paragraph takes focus while the rest of it dims +p2 @editor +fb fb:2026-07-29 est:45m vis:4.1 REAL AND STILL REPRODUCED four days after it was filed MEASURED as a sweep of blockRange over every position of a paragraph which is a pure function of a document and a position so a state and a number is the honest instrument and the answer was one mismatch out of 211 positions THE LAST ONE ROOT CAUSE resolveInner was asked with side plus one and at the END of a block that resolves to whatever STARTS at that position and at the end of a paragraph nothing does so the walk reached the root having found no block and fell through to the caret's own line THE EXACT MIRROR of the bug continuation.ts already documents where side minus one at a line START resolves to the line before which is why the plus one was chosen in the first place NEITHER SIDE IS RIGHT ALONE FIXED topLevelAt asks forward then backward and only one of the two can be inside a block because the position where both fail is a blank line which blockRange deliberately gives a target of its own and a test pins that half so the fix cannot quietly take it away NOT AN EDGE CASE IN USE the end of a paragraph is exactly where the caret sits after every keystroke typed there so the whole paragraph dimmed to one line while you typed which is the report word for word ALSO FIXES lists and fences whose last position collapsed the same way and the option+arrow block jump which asks blockRange for the same answer AN EXISTING SPEC LOOKED LIKE IT COVERED THIS the target is the whole block not just the caret's line clicks into the MIDDLE of a paragraph and the middle always worked THE READER CANNOT HAVE THIS BUG its blocks are DOM elements and there is no position-at-the-end-of-a-block to ask about so for once the two surfaces did not diverge
x 2026-07-29 (B) 2026-07-29 Hotkeys register at app scope not inside the editor cmd+i does nothing when the window is focused but the caret is not +p2 @shell +fb fb:2026-07-29 est:30m vis:7.1 FIXED BY the registry which listens once on the window
x 2026-08-01 (B) 2026-07-29 Open the editor with its first block on the anchor the way the reader's initFocus does +p2 @editor +found found:2026-07-29 est:20m vis:4.1 NOW ONE CALL scrollToAnchor is exported from reader/focus.ts as of 2026-07-30 so the estimate is generous MEASURED FIRST the reader centres its first block exactly on the anchor 300 against 300 having scrolled 105 and the editor never scrolled at all so its first block sat 119px BELOW the anchor at a 900px window NEARLY RIGHT BY ACCIDENT which is the interesting part the leading gutter is 34vh and the anchor is at one third so a surface that never scrolls lands close to the anchor by arithmetic coincidence and the two drift apart the moment either number is edited THE GUTTER IS THE SPACE THE SCROLL MOVES INTO not a substitute for the scroll DONE openOnAnchor in the focus plugin constructor in a frame because it is a measurement and skipped entirely if a caret arrived first since 4.1 gives the caret the target for good NOT A SECOND COPY OF THE ARITHMETIC scrollToAnchor took an Element and the editor has none since a paragraph is several cm-line divs and CodeMirror answers in coordinates so it now delegates to scrollBoxToAnchor which takes a box and both callers reach the same statement THE FIRST BLOCK by blockRange which is what focus itself paints not the first line which differs whenever a document opens on a paragraph MY SPEC WAS WRONG BEFORE THE PRODUCT WAS it compared border boxes across the two surfaces and reported a 14px error that did not exist because the reader spends block space as MARGIN which 5.3 needs for collapsing and the editor spends it as PADDING because CodeMirror's height map cannot see margin so a cm-line box is legitimately taller than the equivalent reader element RULE any cross-surface geometry comparison measures the TEXT through a Range not the element box because the text is the one box both surfaces agree on and it is also what a reader actually sees on the anchor MEASURED AGAIN both surfaces put the first block's text at top 280 bottom 319 centre 300 against an anchor of 300 and both scroll by exactly 105 RED FIRST PROVED by commenting out the call and watching all three go red JSDOM coordsAtPos throws there because a jsdom Range has no getClientRects so the guard is that a surface with no height has no anchor to open on which is a real precondition rather than a swallowed exception and it is true of a detached or hidden editor too
x 2026-08-01 (B) 2026-07-30 Nothing bypasses scrollToAnchor or the tests anchor helper scrollIntoView block center silently means the midpoint +p4 @editor +found found:2026-07-30 est:20m vis:12 SEEN one product function and two specs used that phrase to mean put this on the focus anchor which was the same place until the anchor moved then two link specs failed reporting a colour problem that did not exist WORTH a grep NARROWER NOW 2026-08-01 the editor's opening scroll went through scrollBoxToAnchor rather than growing its own copy and the editor fixture gained the same anchorY hook the reader has so a spec can ask rather than recompute WHAT IS LEFT is the grep itself GREPPED 2026-08-01 and the product is CLEAN the one scrollIntoView left in it is focusIndex with block nearest which is deliberate and documented stepping through a document should not yank the page when the next block is already on screen and it claims nothing about the anchor the four scrollIntoView true in motion.ts and continuation.ts are CodeMirror keeping the caret visible which is not an anchor claim either THE BYPASSES WERE ALL IN TESTS AND ONE WAS MINE FROM THE DAY BEFORE editor-typewriter.spec.ts computed box.height / 2 by hand which is a second copy of TYPEWRITER_RATIO written while closing the task that warns about exactly this FIXED typewriter.ts now exports typewriterY the counterpart of the reader's anchorY and the fixture exposes it so the spec asks PROVED by moving the ratio to one quarter and watching five of six tests follow the product automatically where before they would have compared against their own number THE HELPER WAS ONLY HALF FIXED tests/e2e/anchor.ts asked for the ratio through anchorY and then hand-wrote rect.top plus rect.height over two minus it around the answer which is this file holding its own copy of what ON THE ANCHOR MEANS it would go on agreeing with itself if the product ever aligned block tops rather than centres FIXED the reader fixture exposes putOnAnchor and both helper scrolls call it so the arithmetic exists once LEFT ALONE ON PURPOSE focus-scroll.spec.ts and heading-hierarchy.spec.ts measure a block's distance from the anchor by hand but they ASK anchorY for the position and a box centre is not a design decision and the several rect.height over two in editor-caret and inline-code pick a click point in the middle of an element which has nothing to do with the anchor
x 2026-08-01 (B) 2026-08-01 A cross-surface geometry comparison measures the text through a Range not the element box +p4 @editor +found found:2026-08-01 est:20m vis:12 SEEN the reader spends the space between blocks as MARGIN which 5.3 needs for collapsing and the editor spends it as PADDING because CodeMirror builds its height map from getBoundingClientRect which cannot see margin so a cm-line border box is legitimately taller than the equivalent reader element COST a spec that compared the two border boxes reported a 14px opening error that did not exist and sent me looking for a product bug that was not there while the product had already put both surfaces on exactly the same scrollTop RULE the text is the one box both surfaces agree on and it is also what a reader sees sitting on the anchor WORTH a helper beside the tests anchor module so the next comparison starts from it DONE tests/e2e/geometry.ts beside anchor.ts which owns WHERE the anchor is while this owns WHAT you are allowed to measure against it textBox ranges over a node's contents so padding margin and borders are excluded BY CONSTRUCTION rather than subtracted afterwards and a subtraction would have to know which surface it was on which is the knowledge the helper exists to remove FIRST_BLOCK names both selectors since a reader block is an element and an editor block is a run of cm-line divs so a spec cannot quietly measure a p against a LINE which are not the same unit SWEPT THE SUITE FIRST fifteen specs visit both surfaces and only editor-open compares GEOMETRY across them the rest compare computed styles which are unaffected and the rect calls in editor-blocks editor-headings and editor-surface are all WITHIN one surface marker widths and a mark against its own line SO NO LATENT COPIES OF THE MISTAKE existed and the deliverable really was the helper and the reasoning rather than a repair PROVED THE RULE IS LOAD BEARING by making textBox measure the element box the way the broken version did and watching the cross-surface case report exactly 14px again which is the same phantom error to the pixel NOT A STYLE PREFERENCE the reader uses margin because 5.3 needs adjacent blocks to collapse into one gap and the editor uses padding because CodeMirror's height map is built from getBoundingClientRect and cannot see margin so both are right and the border boxes genuinely differ
x 2026-08-01 (B) 2026-07-28 Intraword identifier underscores stay literal not emphasis +p2 @editor sess:3.2 est:20m ref:F06 vis:6.1 F06 DID NOT REPRODUCE AND COULD NOT BE TESTED which are two different findings and the second is the one that mattered MEASURED FIRST lezer parses intraword underscores exactly as CommonMark requires so no Emphasis node exists for HEADING_SENTINEL_01 nor for some_file_name.ts nor for --max_old_space_size=4096 THE GUARANTEE IS THE PARSER'S not anything this repo performs BUT THE EDITOR APPLIED NO EMPHASIS STYLING AT ALL so the identifier is not emphasised was true of every run of text on the surface and a test for it could not fail WHICH WOULD HAVE BEEN A VACUOUS COMPLETION exactly the trap the editor specs task keeps collecting THE REAL DEFECT was the other half the reader italicises emphasis because a browser italicises em by default and the italic face happens to ship while the editor has CodeMirror marks rather than elements and got nothing so one construct had two answers again AND --type-prose-emphasis-family style weight sat in tokens.css consumed by NO RULE which is precisely where --text-link was before links were built a design decision recorded and never applied DONE Emphasis and StrongEmphasis marked whole with EmphasisMark quieted on top which is the inline-code shape exactly since 6.1 puts emphasis delimiters in the stays-literal list so what changes is the face not what is on screen NOT RETURNING from that branch unlike the others because emphasis nests and strong containing italic needs the walk to continue CSS WENT STRAIGHT INTO md.css rather than reading.css and being moved later which is the first construct to follow that standing note instead of proving it again FONT FAMILY IS NAMED beside font-style because 5.2 says the drawn italic face never a slant applied to the upright and a fallback stack can satisfy the style by synthesising one THE CONTRAST IS THE TEST a real emphasis run and the identifier in the SAME PARAGRAPH measured by the same function so the two assertions cannot both pass unless the surface genuinely tells them apart NOT DONE bold-italic has no shipped face so strong containing emphasis would synthesise filed rather than fixed
x 2026-08-01 (B) 2026-07-29 Code editing for non-markdown files with language detection +p2 @editor sess:3.3 +fb fb:2026-07-29 est:45m vis:6.2 SEEN html and ts parsed as markdown THE HARM IS NOT MISSING COLOUR it is markdown RULES BEING APPLIED TO SOURCE a hash line became a 30px H1 brackets became a link with its destination HIDDEN which loses a URL out of the file being edited an identifier's underscores became italic a row of dashes became a horizontal rule and a pipe row became a table THE FILE WAS NOT UNSTYLED IT WAS REWRITTEN ON SCREEN DONE language.ts maps an extension to whether it is markdown and to a grammar and editor.ts gates notation tables notation-rows markdownStructure and listIndentation on the one boolean ONE CONDITION not a check inside each extension because they all read the same parse tree so turning half of them off would leave the other half decorating a tree that is no longer there ENTER AND TAB ARE MARKDOWN TOO continuing a list marker and nesting one are 6.1 behaviours and performing them in a TypeScript file is the same defect as drawing a heading in it NO PATH REACHES THE EDITOR the caller resolves languageFor(path) and passes the answer because editor.ts already records that it holds text and a caret not a path and detection is not a reason to break that A FILE WITH NO EXTENSION IS CODE since LICENSE and Makefile are not CommonMark and the safe direction for a file we cannot identify is to do LESS to it A DOTFILE IS A NAME not an extension so gitignore is not a type SPEC TENSION FILED 6.2 asks for language-appropriate highlighting and DESIGN.md 5.2 fixes the inventory at Rust and says an absent or unknown language remains honest monospace rather than receiving misleading language colour ONLY ONE OF THOSE CAN BE BUILT TODAY and widening the inventory means new grammars new colour categories and a design change so it is a DECIDE below not something I did quietly A RUST FILE gets exactly what a rust fence gets since both read codeLanguages so the two cannot be coloured by different opinions TEST SPLIT the extension map is a pure function so it is nine unit tests and the browser proves only that the surface acts on the answer CONTROL CAUGHT A REAL MISTAKE the anti-vacuity assertion that every selector I claim is ABSENT in code must be PRESENT in markdown went red on links rules and tables because CodeMirror VIRTUALIZES EVEN AT A 9000px VIEWPORT so constructs near the end of the fixture are not in the DOM at all which is the trap already filed as the scroll-until-built task biting again WITHOUT THAT CONTROL the code assertion would have passed on three selectors that match nothing anywhere RED FIRST PROVED by forcing the fixture back to markdown and watching four go red
x 2026-08-01 (B) 2026-07-28 Typewriter mode holds the caret line vertical position +p2 @editor sess:3.4 est:45m vis:6.1 DONE typewriter.ts a StateField for the flag on raw.ts's reasoning it is per document so two windows can differ and a ViewPlugin that pins on docChanged selectionSet or the effect that switches it on AND NOTHING ELSE which is the whole of 7.6's manual scrolling is always allowed re-pinning on scroll would make reading ahead impossible which is 4.1's own distinction applied to the scrollbar instead of to focus THE MIDPOINT NOT THE ANCHOR 7.6 names both positions in the same paragraph as different numbers the anchor is a third of the way down where the eye rests reading and this is the midpoint where a hand wants the line it is typing NOT A CONTRADICTION and not a leftover from before the anchor moved ONE ARITHMETIC scrollBoxToAnchor now delegates to scrollBoxTo which takes the destination so the reader's anchor scroll and this share the subtraction rather than the second caller inventing its own THE BUG WORTH THE SESSION the first version bounced the caret between the top and the bottom of the window while typing measured at 400 then 204 then 624 then 44 then 16 then 812 CAUSE CodeMirror's measure phase runs EVERY QUEUED READ AND THEN EVERY QUEUED WRITE and typing dispatches a transaction per character so each write applied a delta computed from a rect measured before the previous write had scrolled and the corrections compounded FIXED one outstanding request at a time which loses nothing because the measure phase runs after the DOM is up to date for the whole batch so the one surviving read sees the final caret FOUND BY MEASURING nine keystrokes and printing the caret each time rather than reasoning about it CARET HAS NO ELEMENT there is no drawSelection extension so no cm-cursor exists anywhere the browser draws the caret and editor.css colours it with caret-color so the spec reads the NATIVE selection range which also gives the caret's ROW rather than its line box which matters on a wrapped line 22px against 56px CHORD Mod-Alt-t provisional like Mod-e and Mod-i and paired with word wrap's Mod-Alt-z since both change how the document sits rather than what it says AVAILABLE ONLY WITH A CARET per 7.6 which the registry makes honest in the Reference NOT BUILT the multiple-columns clause of 7.6 since columns are an unbuilt +p4 task
x 2026-08-01 (B) 2026-07-28 Word wrap setting shortcut and persistence agree +p2 @editor sess:3.5 est:30m ref:F03 vis:6.1 HALF DONE 2026-07-31 the command and the shortcut and the Reference row agree now and a spec checks the row what is left is the setting surface and persistence which is where 4.4 lives COMPLETED 2026-08-01 the persistence half NO PREFERENCE MECHANISM EXISTED AT ALL not even for theme which is only a specimen page control so this is the first one DONE suite/preferences.ts with a named accessor per preference rather than a stringly-typed key at each call site since two call sites holding one string is how a preference quietly becomes two IN THE SUITE NOT THE MINI APP because 7.6 calls it a suite preference applied to every document and a preference owned by zd md would have to be made again in the next tool which is what 3 says these are not LOCALSTORAGE rather than a file through platform.ts since it is durable in a webview and a browser alike which keeps the dev pages honest a preference that only persisted in the packaged app could not be looked at while building one SESSION 4.4 may want a file behind the Settings surface and these accessors are the seam that makes that a change to one file NOTHING THROWS reads fall back to the default and writes fall back to memory because a webview with storage disabled must still let you turn wrapping off for the rest of the session THE IN-SESSION MAP IS WHY without it a failed write would be undone by the very next read so the toggle would appear not to work AT ALL rather than merely not to persist which is a much worse failure than the one that actually happened THE EDITOR STILL KNOWS NOTHING it takes wrap as an option the way it takes language and the caller reads the preference and records the new value in the same statement that toggles it so the shortcut and the stored value cannot drift RED FIRST PROVED by pinning the fixture to wrap true and watching the reload case and the every-document case go red THE CONTROL a reload with nothing stored opens wrapped since every other assertion would also pass on a surface that had simply stopped wrapping WHAT IS STILL OPEN the visible Settings row which is the settings surface task in phase 4 this is the setting itself not a control for it
x 2026-08-01 (B) 2026-07-28 Detect and reconcile external changes to an open file +p2 @editor sess:3.1 est:30m vis:6.3 THE GUARANTEE IS THE LAST CLAUSE not silently clobbered and detection is in service of it the failure this prevents is a save that throws away work nobody knew about DETECTION IS A STAMP not a watcher file_stamp returns modified time AND length or None if the file is gone and a watcher would be a plugin a background thread and a stream of events to debounce none of which answers the only question that matters is the file still the one I read BOTH FIELDS because either alone lies a coarse filesystem clock puts a fast rewrite of the same length in one tick and a length is unchanged by swapping one character for another together they miss only a same-length edit inside one tick which needs a machine writing the file rather than a person A MISSING FILE IS None NOT AN ERROR it is a real state the document was deleted or moved while open THE DECISION IS A PURE FUNCTION reconcile.ts takes what we knew what is there and whether the buffer is dirty which is what makes the case that matters testable with no filesystem no clock and no second process THE ASYMMETRY IS THE DESIGN reloading a CLEAN buffer loses nothing so it happens silently reloading a DIRTY one destroys work so it never happens here at all the person is told and decides SAVE IS CHECKED REGARDLESS OF DIRTY because saving a clean buffer over someone else's edit is the same loss and is exactly what a check phrased as only warn if I have changes lets through so saveWouldClobber does not take dirty at all CHECKED ON WINDOW FOCUS and only then since edit elsewhere and tab back is the whole reported shape and 10 makes idle cost part of the design so a timer polling the disk forever would be the wrong trade SAVE IS NOW ASYNCHRONOUS it asks the disk before writing which broke an existing test that asserted before the write landed FIXED in the test with the reason written down NOTICE goes through the status strip 7.10 already owns rather than a second surface which would be the chrome 7.4 rules out NOT DONE no merge 6.3 says reconciled and a three way merge is a product of its own so what this promises is the part of that sentence that prevents data loss and it says so rather than implying more VERIFIED 5 rust tests on the stamp 13 unit tests on the decision and 2 integration tests on the refusal RED FIRST PROVED by disabling the guard and watching the refusal go red

## 2026-07-30

x 2026-08-01 (A) 2026-08-01 A failed or refused save still reports the document as saved +p2 @editor +found found:2026-08-01 est:60m vis:6.3 audit:H1 DATA LOSS AND IT IS A REGRESSION I INTRODUCED the clobber refusal and the async save landed 2026-08-01 and editor.ts marks written before the write is even attempted so all three failure paths lie THE REFUSAL says saved for a buffer never written A WRITE ERROR disk full permissions read-only directory gone rejects inside a void async with no catch so the user sees nothing and quitting loses the work AND TWO RAPID cmd+s RACE each does stamp-check write re-stamp with no serialisation so the second can stamp after the first wrote but before known updates and the user gets a spurious changed-on-disk warning for their own write FIX per the audit invert the ownership of saved onSave returns a promise and written plus recheck move to the RESOLUTION path while a rejection keeps the buffer dirty and says one calm sentence THEN serialise saves by chaining the next behind the in-flight one which removes the race without locks RED FIRST a unit test that a rejecting onSave leaves isDirty true pins the contract SEE docs/audit/2026-07-30-src-architecture-audit.md DONE exactly as the audit recommended save now returns a promise and written plus recheck moved to the RESOLUTION path so a handler that resolves false refused and one that rejects failed and either way the buffer stays honestly dirty THE REJECTION IS SWALLOWED rather than rethrown because whoever handed us onSave is the only party that knows what went wrong and owns the notice while a command that dispatched a key has nowhere to put an exception THE CALLER now catches the write error names the reason and says your work is still here and still unsaved and returns false SERIALISED by chaining each save onto the one in flight with a resolved promise as the resting state so there is no is-one-running branch at all FOUR NEW UNIT TESTS three red first a failed write a refused save and the interleaving which printed start:8 start:9 end:9 end:8 as the race itself PLUS A CONTROL that a successful write does become clean since every other assertion is still dirty which is trivially true of an editor that never becomes clean PROVED by moving the claim back before the write and watching both dirty tests go red TWO TEST BUGS OF MY OWN ALONG THE WAY the serialisation gate was released synchronously before the handler had created it which deadlocked and the reader helper counted exactly two microtask turns which was right only while the chain was two deep so it now flushes with a macrotask since counting turns is the same brittleness as counting frames NO WAY TO DIRTY A DOCUMENT existed in the unit suite so the test reaches the view through EditorView.findFromDOM which is CodeMirror's own documented way back from an element rather than a hook added to the product for a test
x 2026-08-01 (A) 2026-08-01 Rendered tables stay at full contrast and never dim when out of focus +p2 @editor +fb fb:2026-08-01 est:30m vis:4.1 SEEN in render mode a table is the one construct that ignores the dim entirely so a document with a table has one bright block wherever you are reading WHICH IS THE OPPOSITE of 4.1's whole claim that everything not the target is visibly dimmed LIKELY the widget renders reader DOM inside the editor so the focus attribute on the cm-line never reaches it the same class as the cell styling defect closed 2026-07-31 THE SUSPECT WAS RIGHT FOR ONCE but the measurement was worse than the report the table carried NO data-focus attribute at all and painted rgb 36 37 34 which is the TARGET colour byte for byte while the lines around it sat at srgb 0.702 so it was not merely undimmed it was the brightest thing on screen wherever you were reading CAUSE IS STRUCTURAL a table is Decoration.replace block true so CodeMirror renders it as a SIBLING of cm-line rather than inside one and a line decoration cannot reach it because the lines it would apply to are the ones the widget replaced DONE paintWidgets in focus.ts asks CodeMirror where each non-line child of the content sits through posAtDOM and writes target or context onto it in a requestMeasure read and write pair A DOM WRITE RATHER THAN A DECORATION because the range is already spoken for by the replacement and there is no second decoration to express it with NOT A LIST OF WIDGET CLASSES a child of the content that is not a line is a widget by construction so the next one inherits this CSS IN editor.css rather than md.css since the reader has no widgets and the where selector keeps the specificity level with the two line rules beside it ONE EXISTING TEST HAD TO CHANGE and it is a real correction not a workaround a link in a cell reads as activatable asserted text.link on a table that NEVER DIMMED so it measured full contrast by accident now that widgets dim an out-of-focus link is dimmed which is exactly what md.css already says a dimmed link is still a link but it is dimmed first SO THE CLAIM GAINED A WHEN it focuses the table first and the dimmed case belongs to the new spec TWO TEST BUGS OF MY OWN the colour read came back oklab because a colour mid transition serialises that way and then the poll never settled because the helper re-scrolled the document on EVERY poll which restarted the transition it was waiting on THE MEASUREMENT WAS RE-CREATING THE CONDITION IT WAS WAITING ON split into revealWidget once and readWidget per poll PROVED by commenting out the paintWidgets call and watching all three go red
x 2026-08-01 (A) 2026-08-01 Quitting with unsaved changes does not ask +p2 @shell +fb fb:2026-08-01 est:45m vis:6.3 SEEN no confirm exit without save on quit SO CLOSING THE WINDOW THROWS THE BUFFER AWAY which is the same class of loss as H1 from the other end 6.3 says unsaved state is visible without adding chrome and says nothing about quitting so this needs the decision as well as the dialog NOTE a Tauri close request is interceptable in the Rust shell so the guard belongs there with the frontend answering whether the document is dirty THE SPEC SAYS NOTHING ABOUT QUITTING one line about unsaved state being visible and that is all so the decision was genuinely open DECIDED A SECOND ASK RATHER THAN A DIALOG and the reasoning is DESIGN.md 3 says chrome is absent and text before controls and the ONE transient surface the design has is explicitly not interactive since the Reference is a glance not a mode SO A MODAL WITH BUTTONS would be new visual language for something the 7.10 status strip already covers and it would add a dialog dependency the app does not have ASKING TWICE is the smallest thing that cannot lose work and it is the convention every terminal editor already taught SAY SO PLAINLY this was my call not the spec's and it is reversible if it reads wrong in use DONE the shell refuses EVERY close unconditionally and emits to the frontend which is the only side that knows whether anything is unsaved UNCONDITIONAL ON PURPOSE a shell that closed when it BELIEVED the document was clean would keep a second copy of a fact it does not own and that copy is wrong exactly when it matters THE ARMING CLEARS ON SAVE rather than on a timer since a warning that expired on its own would leave the next close silently unguarded and the reader would have no way to know which state they were in FOUR TESTS red first three of them plus a control that a CLEAN document closes on the first ask without making anyone press twice for nothing PROVED by disabling the guard and watching three go red PLATFORM GAINED onCloseRequested and closeWindow and the browser shell answers honestly that nobody ever asks since a tab is not this app's window to close and beforeunload is the platform's own affair NOT VERIFIED IN THE REAL SHELL the e2e suite runs in a browser where there is no close event so the Rust half is covered by cargo build and by reading and the whole path wants one manual try
x 2026-07-30 (A) 2026-08-01 A paragraph and the code block it introduces take focus together +p2 @editor +fb fb:2026-08-01 est:45m vis:4.1 ANSWERS THE DECIDE below which asked whether this should happen and the answer is yes reported a second time with a blocking bang on the same Needs Node and Rust line WAS MEASURED 2026-08-01 as 7.6 working exactly as written paragraph means one semantic block and a fence is a different block SO THIS IS A SPEC CHANGE not an implementation fix and 7.6 has to say the new rule before the code does DECIDE WHICH RULE a lead-in pairing where a paragraph immediately followed by a fence focuses with it or a fourth granularity since section is too wide DECIDED THE LEAD-IN PAIRING and the argument that settled it is that a fourth granularity is a new selectable level and 7.9's settings surface is unbuilt so it would ship an answer nobody can turn on WHICH IS THE SAME OBJECTION already recorded against reaching this through section granularity SO THE PAIRING IS A CHANGE TO WHAT PARAGRAPH MEANS rather than a new name beside it and it is one clause in 7.6 next to the list clause that is already there for the same reason a list is one block because it reads as one thing BOTH DIRECTIONS not an arrow since an arrow puts the lead-in out the moment the caret enters the fence which is the reported loss seen from the other end ONLY A CODE BLOCK fenced or indented because the block after this one as a general rule widens until it IS section granularity and two paragraphs in a row are two thoughts A LIST AFTER A PARAGRAPH IS LEFT ALONE until someone reports it SPEC FIRST DESIGN.md 7.6 and vision 4.1 both say the rule and the code cites them RED FIRST four unit claims went red before the change and four e2e went red across BOTH SURFACES when the rule was disabled while the three controls stayed green BUILT ON BOTH the editor walks the parse tree and the reader walks rendered elements and the rule rather than the code is what is shared exactly as sectionRange already is BECAUSE A READER THAT DISAGREED with the editor about what a target is would be the list-focus defect back again THE OLD SPEC HAD A TEST focus-block-boundary.spec.ts was written 2026-07-30 as coverage for the rule that a fence is a different block and every assertion in it inverted which is what a test written against a spec section by name is supposed to do
x 2026-07-30 (B) 2026-08-01 With wrapping off there is no margin past the longest line so the last character sits on the screen edge +p2 @editor +fb fb:2026-08-01 est:20m ref:F03 vis:5.3 SEEN scrolling right puts the final character hard against the edge which is hard to read THE INSET EXISTS on the vertical axis and on the left so this is the trailing edge of the scrollable extent rather than a padding value being wrong NOTE overflow-x auto was added 2026-07-31 and a right padding on the column does not extend scroll width the way a margin does which is the same distinction the focus gutters turned on THE NOTE HAD IT BACKWARDS margin is the thing that does NOT work here and padding is what does which is the opposite of the vertical fix and worth knowing as a pair MEASURED FIRST at a 600px window scrollWidth 761 against a 729px content box which is the 32px LEADING padding and nothing else and the last character landing on the frame at -0.3px A SCROLL CONTAINER'S scrollable overflow is its padding box plus the BORDER boxes of overflowing descendants so trailing padding is not added past content that already ran beyond it and a trailing margin on a descendant is not in it at all TRIED AND MEASURED margin-inline-end on cm-content changed scrollWidth by exactly zero and padding on md-editor cannot help either since with wrapping off the lines are wider than that column DONE padding-inline-end on cm-content which is the element whose width IS the horizontal extent because CodeMirror gives it a min-width of the longest line WRAPPING OFF ONLY through cm-lineWrapping which is CodeMirror's own class added by the extension the toggle reconfigures so the selector tracks the real state rather than a copy NOT A SHORTCUT with wrapping on the same padding would either narrow the 5.3 measure or push content past the window and the document cannot drift sideways is the resting state claim md.css makes RED FIRST one assertion with two preconditions stated rather than assumed that something overflows and that the surface actually reached its end THE FIRST VERSION OF THE SPEC PASSED AT 900px by 30px of accident because the fixture's longest line is still narrower than that window with wrapping off so it asserted an inset on a document that never reached the edge WHICH IS THE VACUOUS-ASSERTION TRAP the editor spec task already lists three times MEASURED ON THE TEXT through a Range since a cm-line box fills the column and reports the column's edge wherever the characters stop THE LEADING CONTROL measures the COLUMN's box not the leftmost glyph because notation markers hang outside the column by design and at 600px the leftmost glyph is 16px PAST the window frame which is filed as a finding rather than fixed here
x 2026-07-30 (B) 2026-08-01 The option and arrow block jump has no row in the Shortcut Reference +p4 @editor +fb fb:2026-08-01 est:30m vis:7.1 SEEN reported as it should be listed in the shortcuts listing AND IT CANNOT BE TODAY because the jump is a CodeMirror keymap rather than a registry command which is deliberate for Enter and Tab since they have no chord DECIDE whether a chord that a person would look up belongs in the registry even when it is text motion because 7.1 says the Reference renders the registry and a binding outside it is invisible by construction DECIDED YES and the line is not whether a chord moves the caret it is whether someone would go LOOKING for the key CMD+ARROW STAYS A KEYMAP because it is the platform's own line boundary motion and what motion.ts holds is a CORRECTION to a binding macOS already taught so a row saying it moves to the start of the line would tell a reader what they arrived knowing SAME FOR Enter and Tab keys doing their obvious job in context with no chord to remember A FOCUS BLOCK JUMP IS THE OPPOSITE a product feature with a name on a chord nothing else on the platform would suggest and it is the one that got reported missing NO SECOND OPTION EXISTED anyway since 7.1 says the Reference renders the registry and is not a hand maintained list that drifts so a binding outside the registry cannot appear in the Reference at all and adding a row by hand IS finding F16 DONE jumpFocusBlock is a plain function now the editor handle exposes jumpBlock and index.ts registers the two chords the same way save and wrap and typewriter are registered THE KEYMAP ENTRIES WERE REMOVED rather than left beside the registry because preventDefault does not stop propagation so both would have run and one press would have jumped twice AVAILABLE ONLY WITH A CARET since 4.1 makes placing one a one way door out of anchor following and a motion key must not cross it on the reader's behalf WHICH ALSO PRESERVES today's behaviour exactly the key did nothing before a click into the document and does nothing now and 7.1 wants that said out loud rather than listed as working RED FIRST two claims went red the row and the honest unavailability while the SIX existing behaviour tests stayed green which is what proves the binding moved rather than broke PROVED THE SECOND CLAIM separately by setting available to true and watching only it go red THE CHORD IS ASSERTED as a modifier and a direction rather than against a literal label because chordLabel renders per platform and re-deriving it in the test would be a second copy of that function FILED NOT FIXED the dev fixture keeps a SECOND copy of the command list so this meant writing the same two register calls twice and every Reference test runs against the fixture's copy
x 2026-07-30 (B) 2026-08-01 The option and arrow block jump should land the new block on the focal point +p2 @editor +fb fb:2026-08-01 est:30m vis:4.1 SEEN it jumps correctly but leaves the block wherever it happened to be on screen SO THE TARGET AND THE ANCHOR DISAGREE after every jump which is the thing 4.1 asks to keep together NOTE scrollBoxToAnchor already exists and openOnAnchor already uses it so this is one call rather than new arithmetic THE NOTE WAS RIGHT it is one call MEASURED FIRST at a 1100 by 700 window a jump left the landed block 97px off the anchor CAUSE the dispatch carried scrollIntoView true which is CodeMirror's somewhere visible so a block already on screen does not move at all DONE openOnAnchor is generalised into a public putOnAnchor that takes a resolver and the jump calls it through a new exported scrollBlockToAnchor A RESOLVER RATHER THAN A RANGE because the opening scroll has to be able to stand down once it sees a caret arrived first and that decision belongs inside the measure phase THROUGH THE PLUGIN rather than reimplemented since it owns the coalesced scrolling measure and the remeasure that follows a scroll and both matter here because a HELD key asks for this faster than the screen refreshes WHICH RETIRES the note on that measure that said it was safe because nobody calls it twice this is the second caller INSTEAD OF scrollIntoView not alongside it since two scroll sources for one press is the compounding correction shape that made the typewriter caret bounce down the window RED FIRST one assertion at 97px off with TWO PRECONDITIONS stated that the surface was not clamped at either end because scrollBoxTo is a scrollTop plus equals and at either end of the range the claim becomes untestable rather than false ITS OWN VIEWPORT and that is the point rather than housekeeping the rest of that file runs at 9000px tall so the whole fixture fits on screen and NOTHING CAN SCROLL which would have made every assertion pass without measuring anything MEASURED ON THE TEXT through a Range rather than on the line boxes since a heading carries 5.3's space as padding so its box top is a rhythm value above its first glyph and box measurement would read as a failure on exactly the blocks that are correct PROVED by putting scrollIntoView back and watching only the new claim go red while all fourteen anchor and open assertions stayed green which is what says the refactor did not move the opening scroll
x 2026-07-30 (B) 2026-08-01 Auto pair brackets quotes and backticks and wrap a selection when one is typed +p2 @editor +fb fb:2026-08-01 est:45m vis:6.1 SEEN typing an opening bracket should create the closing one and typing one over a selection should wrap it NOTE @codemirror/autocomplete ships closeBrackets which does both and is the same shape as the lang-markdown decision take the library and own only what it gets wrong CHECK it against the notation decorations first since a hidden fence row is an atomic range and an inserted closer near one is exactly where the fence typing bug lived THE CHECK WAS THE WHOLE SESSION and it found TWO collisions not one both of them the same shape pairing FINISHES a construct the writer had only started and the notation renders it immediately DONE closeBrackets taken whole plus pairing.ts holding the one veto and notation.ts declaring which characters pair in markdown ALREADY A TRANSITIVE DEP of lang-markdown so it is now a direct one at the version already installed COLLISION ONE THE LINK typing see bracket here bracket paren http x dev paren one character at a time produced see bracket here bracket paren paren http x dev paren BECAUSE the auto-inserted closing paren COMPLETES the link the notation plugin renders it that same update the destination becomes a hidden ATOMIC range and the caret is pushed out of it so every character after lands outside the link FIXED by taking the opening paren OUT of markdown's bracket set which is language data so a code file keeps it WITHOUT PAIRING THERE IS NO COLLISION AT ALL since a link is not a link until its closing paren so nothing renders while it is typed and the caret is already past the construct when it does AND THE PAREN WAS NEVER IN THE REPORT which named square brackets braces backticks and quotes COLLISION TWO THE FENCE typing three backticks mid document produced FOUR the first pairs the second skips its own closer and the third pairs AGAIN because the library's in-a-string check resolves against a tree where inline code elsewhere makes the position look open ON THE LAST LINE OF A DOCUMENT the same three keystrokes came out correct which is the sort of position dependent behaviour that reads as a flake CONSEQUENCE a fence with a language could not be typed at all FIXED by an inputHandler placed BEFORE closeBrackets that plain-inserts a backtick typed onto the end of a run RULE STATED as what the writer is doing a run of backticks is a fence or a multi backtick delimiter and either way it is being BUILT so a closer inserted into the middle is never what was meant SILENT when the library would skip its own closer which is what keeps backtick x backtick to one pair and silent for a selection which is the wrap that was asked for TAKEN WHOLE IS DEFENSIBLE because of a case the library already gets right it refuses to open a quote when the character before the caret is a word character so don't stays don't which in a PROSE editor is the keystroke that matters most and a hand written version would have shipped a doubled apostrophe first RED FIRST six claims went red then two of my own expectations were wrong about the fixture rather than about the feature PROVED BY DISABLING both fixes at once and watching exactly the three tests that broke mid session go red while all twenty five others stayed green
x 2026-07-30 (B) 2026-08-01 The status strip needs a read time and a line count +p4 @shell +fb fb:2026-08-01 est:20m vis:7.10 SEEN show 4m or something like that no details since a time on a stat line reads as a reading time without a label WORDS AND CHARACTERS are already there so this is two more values in statusLine and the read time is words over a rate that has to be named once rather than guessed at each call DONE the line now reads words characters lines read-time and the saved state in that order RAW COUNTS FIRST then the one derived value then the state since the read time is words over a rate rather than a count of anything THE RATE IS 200 WORDS PER MINUTE named once as a constant with the argument beside it it is the commonly cited average for CAREFUL silent reading and careful is the right end of the range for specs notes and agent logs which are read to be understood rather than skimmed PUBLISHING ESTIMATES use 250 to 265 which would make every document look a fifth quicker than it is and erring slow is the honest direction for a number someone uses to decide whether they have time NO LABEL exactly as asked since a duration on a line of counts reads as a reading time without one and a label would be the detail that was asked not to be there ROUNDED UP so a document with a sentence in it reads as 1m rather than 0m because rounding down tells a reader that something takes no time which is the one thing a reading estimate must never say about text that exists HOURS ABOVE SIXTY MINUTES because 10's multi megabyte agent logs reach them and 184m is not a number anyone converts at a glance from a line that leaves after ten seconds with the remainder dropped when it is zero so the common case stays two characters SINGULAR AT ONE LINE because 1 lines is the kind of small wrongness 2 spends its whole list avoiding LINE COUNT IS NEWLINE SEPARATED which is what the editor's own doc.lines says so a trailing newline is two lines and an empty buffer is one since there is always somewhere to type A STATUS LINE THAT DISAGREED with the document it describes would be worse than no count RED FIRST nine unit claims went red before the change PLUS TWO E2E because the unit tests prove statusLine and the TYPE proves a caller cannot forget a field but neither proves the COMMAND hands the strip a real document THE COMMAND'S DESCRIPTION had to change too it said show word count and unsaved state which named one of four values and it is rendered in the Shortcut Reference ONE REFERENCE SPEC located that row by the words word count and now locates it by unsaved state since a locator anchored to the part most likely to be reworded fails for reasons that are not defects
x 2026-07-30 (B) 2026-08-01 Ctrl is treated as Cmd on macOS which shadows standard text editing keys +p4 @shell +found found:2026-08-01 est:30m vis:7.1 audit:M1 SEE docs/audit/2026-07-30-src-architecture-audit.md for the whole finding USER VISIBLE on the primary platform every day and the audit puts it second only to H1 DONE mod resolves per platform now metaKey on macOS and ctrlKey elsewhere WHAT MADE IT USER VISIBLE is that attachShortcuts listens in the CAPTURE phase on window so the registry won before the editor ever saw the key and macOS ships emacs style ctrl+a ctrl+e ctrl+k as system wide text editing keys that defaultKeymap implements SO A MAC USER reaching for end of line toggled raw mode WENT ONE STEP FURTHER THAN THE AUDIT ASKED it recommended requiring metaKey and requiring ctrlKey false on mac and that is only half the rule A CHORD WITH NO MOD AT ALL like Escape is satisfied by does-the-logical-modifier-match whenever the OTHER physical modifier is down so ctrl+Escape would have dropped the caret STATED INSTEAD as this platform's modifier is what the chord says and the other one is up which covers chords with a modifier and chords without FOUND BY READING the predicate rather than by a report which is why it is a test before anyone hits it SIX NEW UNIT CLAIMS four red first ctrl declined on mac cmd still answering on mac ctrl answering elsewhere the Windows key declined elsewhere both modifiers held being a different chord and the Escape control A UNIT TEST NOT A BROWSER ONE for exactly the reason already written in that file about Alt and the composed omega platform modifier composition is what a harness SUPPLIES rather than observes and Playwright's ControlOrMeta resolves to Meta on macOS so the e2e suite goes green on BOTH SIDES of this defect while the key is dead in the product THE 385 E2E ARE THEREFORE EVIDENCE OF NO REGRESSION not evidence of the fix THE PLATFORM IS STUBBED through the navigator currentPlatform actually reads rather than by handing dispatch a platform since a seam added for a test lets the test agree with itself and not with the app THE WHOLE FILE NOW STATES ITS PLATFORM defaulting to mac because DESIGN.md 8 names it primary and every metaKey assertion in it was already written in that platform's terms ONE OLD TEST INVERTED accepts ctrl for mod as well as meta so one entry covers both platforms was the defect written down as a promise and it now asserts the opposite with the old reasoning kept beside it PROVED by putting the old predicate back and watching exactly four go red
x 2026-07-30 (B) 2026-08-01 Data loss warnings ride the ten second status strip and then vanish +p2 @shell +found found:2026-08-01 est:60m vis:7.10 audit:M3 SEE the audit COMPLETES H1's fix since a refusal that disappears after ten seconds is barely better than one that never spoke and 6.2 already specifies a persistent surface for this THE SPEC IS 7.3 NOT 6.2 and it is exact one persistent document-local notice line ABOVE the retained content rendered as text on the document plane rather than decorative chrome which withdraws when the path reappears and NEVER CARRIES A BUTTON DONE notice.ts holds both lifetimes and one appearance the strip exists only after its command and leaves after ten seconds the persistent line stays until the condition that raised it clears TOLD APART BY AN ATTRIBUTE not by a second class because they differ in WHEN THEY LEAVE not in what they look like and a notice that looked different depending on how long it stayed would be inventing a severity language 2 does not have THE ROUTING RULE SAID ONCE in warn a message about work that might be lost persists everything else rides the strip WHICH MESSAGES a refused save a failed write and the reconcile keep and vanished cases all four are evidence that what is on screen is the only copy and evidence that expires is not evidence WHAT STAYS ON THE STRIP the reload notice since nothing was at risk the buffer matched the disk and the close prompt since asking again immediately IS the answer INSIDE THE COLUMN above the first line rather than at the top of the surface because the surface's leading gutter is a third of a window tall so a notice placed there would be scrolled off screen the moment the document opened on the anchor WHICH IS A WARNING YOU HAVE TO GO BACK FOR IT WITHDRAWS on a successful save and on a reconcile that finds nothing wrong which is 7.3's the path reappears CLEARING AN ABSENT NOTICE IS A NO-OP deliberately since every caller clears on its good path and none of them should have to find out whether something had gone wrong first ONE EXISTING TEST INVERTED says so and keeps the work when the write fails asserted the message was on md-status which is the ten second strip and is the defect itself it now asserts the persistent line AND that the strip is empty since a warning on both surfaces would still expire on one FIVE NEW MODULE TESTS plus two new routing tests all red first because the module did not exist THE ONE THAT NEEDED A TEST rather than a reading is one line since a notice that stacked would be the toast 7.10 forbids arrived at by accident and two of these conditions really can hold at once a file can vanish while a save is being refused NEVER CARRIES A BUTTON is asserted rather than trusted because the pull towards and a Reload button is exactly what turns a line of text into chrome PROVED by routing warn back to the strip and watching exactly three go red
x 2026-07-30 (B) 2026-08-01 Boot has no failure surface so a mount error leaves a blank window +p4 @shell +found found:2026-08-01 est:20m vis:7.10 audit:M4 SEE the audit THE CHEAPEST INSURANCE in the whole list per its own ordering TAKEN OUT OF FILE ORDER and the reason is said plainly both open +p2 lines are genuinely not actionable the clickable link is blocked on two unbuilt +p3 navigation tasks and the typewriter judder explicitly asks the human to look and say whether it reads as one SO THE NEXT ACTIONABLE IS +p4 and among those M2 the unscoped filesystem surface carries its own note to defer until a workspace root exists AND THE AUDIT'S OWN PRIORITY TABLE ranks this fourth and M2 sixth so this is the audit's ordering rather than a substitution for it DONE every path out of boot now either mounts something or puts a sentence on the canvas and it NEVER REJECTS which is what makes the void at main.ts's call site honest rather than lucky TWO DOORS not one the audit names launchRequest and that is the first thing boot does but app.mount is a mini app's own code and can fail for reasons the suite cannot anticipate so both are caught THE ORDER INSIDE THE MOUNT CATCH IS THE POINT rather than tidiness the keyboard registry and the Reference are attached BEFORE the mini app mounts deliberately so leaving them would put a Reference over a window holding nothing but an apology and cmd+. would open a sheet listing a mini app that does not exist THE REASON NOT JUST THE FACT since a sentence saying only that something went wrong is the blank window with extra steps ONE STYLESHEET src/suite/boot.css imported from design/index.css beside reference.css which is the established place for a suite surface's CSS ALL IT NEEDS IS THE 5.3 INSET because a sentence flush in the corner reads as a browser error page rather than as this app having something to tell you NO BACKGROUND NO BORDER NO ICON since 7.10 says a notice is a sentence on the canvas and the pull to make failure LOOK like failure is exactly how a calm surface grows a red box THE NO-MINI-APP BRANCH now goes through the same renderer so there is one way boot speaks FIVE NEW UNIT CLAIMS four red first plus a control that a boot with nothing wrong still mounts since every other assertion is satisfied by a boot that always fails PROVED by rethrowing from both catches and watching exactly four go red
x 2026-07-30 (B) 2026-08-01 There is no CI so nothing runs the suite except a person remembering to +p4 @reset +found found:2026-08-01 est:30m vis:12 audit:M5 SEE the audit LOCKS IN everything above it and the suite is 19 seconds so there is no cost argument IN SCOPE DESPITE LOOKING LIKE PHASE 5 the session rule forbids packaging signing coverage thresholds and release evidence and this is none of those four it runs the checks that already exist and the plan itself files it as +p4 DONE .github/workflows/check.yml one job on macos-latest running npm ci then check then playwright install chromium then test:e2e then cargo test and cargo clippy MACOS NOT LINUX because DESIGN.md 8 names it primary the Tauri shell builds natively there without the webkit dependency dance and the e2e suite presses chords Playwright resolves against the HOST OS so a Linux run would exercise a key composition no reader of this app ever uses IT IS THE EXPENSIVE RUNNER and that is the trade CLIPPY WITH -D WARNINGS because plain cargo clippy exits ZERO on every lint it reports so a step that cannot go red reads as covered while covering nothing MEASURED BEFORE CLAIMING clippy was not even installed locally so rustup component add clippy then cargo clippy --all-targets -- -D warnings which came back clean and only then was the strict form written into the file NO RETRIES ANYWHERE playwright.config.ts pins retries at zero and there is one known intermittent spec tracked below at about one full run in four with a note to promote it when it costs a diagnosis rather than a re-run SO RETRIES HERE would take that measurement away and leave the flake permanently invisible WHICH MEANS CI WILL OCCASIONALLY BE RED for that spec and that is the trade the plan already made NO THIRD PARTY TOOLCHAIN ACTION cargo is preinstalled on the runner so the supply chain is checkout setup-node and cache all three GitHub's own WHAT THIS SESSION CANNOT VERIFY is that the workflow passes since that takes a runner and the honest verification is a green run on the first push SO THE GUARD IS THE HALF THAT ROTS SILENTLY six unit claims all red first that the file exists that every npm run in it names a script package.json actually defines that all four audit-named commands appear that clippy can fail that install is pinned to the lockfile and that nobody quietly adds retries THE SCRIPT NAME CLAIM IS THE ONE THAT EARNS ITS PLACE a workflow calling a renamed script fails on the runner days later with a message about a missing script and by then nobody connects it to the rename PROVED by breaking the file four ways at once a dropped e2e step a toothless clippy an unpinned npm install and a renamed script and watching exactly four go red
x 2026-07-30 (B) 2026-08-01 Eight smaller audit findings from L1 to L8 fold into the sessions that touch those files +p4 @editor +found found:2026-08-01 est:45m vis:12 audit:L1-L8 SEE docs/audit/2026-07-30-src-architecture-audit.md THE LIST platform.ts importing a type from a miniapp the dead onStatus option a duplicated comment block dependency pinning that does not match the stated policy absolutize normalising a dot but not a double dot the atomic save changing the identity of special files module level shared state in reader/focus.ts and section granularity walking the whole document per update NOT ONE TASK EACH on purpose since the audit says opportunistic and a line per triviality would bury the plan SPLIT any of them out the moment one is worth a session of its own TAKEN AS A LINE rather than folded in because nothing was going to touch those files before the checkpoint the two open +p2 lines are blocked and the audit's own remaining mediums defer themselves FOUR ARE FIXED L1 FileStamp moved to platform.ts which is its honest home since it describes what the file_stamp command returns and the platform is the BOTTOM layer so a type owned by a mini app and named there meant the next mini app that touches files inheriting a type from md's directory RECONCILE RE-EXPORTS IT rather than only importing since every caller that reconciles also reads stamps and making them import from two files to do one thing trades a layering problem for an ergonomics one L2 the onStatus option deleted it was declared and documented and invoked nowhere in src or tests and the status command reads text and isDirty directly which is the better design L3 the file-as-we-last-agreed paragraph written out twice collapsed to one plus a pointer L4 types/markdown-it moved to devDependencies where a type package belongs and .npmrc pins new adds since AGENTS.md says pin versions and every range was a caret THE EXISTING RANGES ARE LEFT ALONE deliberately rewriting them all is a dependency bump session with its own verification and the lockfile plus npm ci in CI is what makes an install reproducible today TWO ARE DECISIONS AND ARE NOW RECORDED WHERE THE CODE IS rather than fixed L6 a rename replaces the PATH so a symlink becomes a regular file hard links break and watchers see delete-then-create and the trade is taken on purpose because none of the three loses DATA while writing in place loses the whole document on a crash mid-write ALSO fs::canonicalize before writing fixes the symlink case for free and arrives with M2's path scoping so revisit it there L8 sectionRange walks every top-level block per recompute against ADR 0003's work-proportional-to-the-viewport budget and it is LEFT ALONE because AGENTS.md forbids optimising without a profile and section granularity is reachable only through the dev fixture so a profile of it would be a profile of nothing anyone uses THE CHEAP ANSWERS ARE WRITTEN DOWN early-exit past the viewport or memoize by tree identity TWO ARE DEFERRED WITH REASONS L5 the double-dot normalisation because the audit itself says canonicalising at the scope boundary resolves it and that boundary is M2 which waits for a workspace root so doing it now means writing path canonicalisation twice L7 the module-level scrollIsOurs because the audit says consequence today none one window and worth aligning when a second window or split lands ONE NEW GUARD red first the platform imports nothing a mini app owns stated about the DIRECTORY rather than about FileStamp so the second one is caught as well as the first PLUS A CONTROL that the file really does import things since no import matches is trivially true of a file the test failed to read PROVED by reintroducing the inversion and watching it go red MY OWN MISTAKE ALONG THE WAY the git checkout that removed the probe also removed the uncommitted L1 fix from the same file and the typecheck caught it in one line
x 2026-08-01 (B) 2026-07-30 A guard that catches any block widget claiming vertical space with a margin the way the token guard catches a stray hex +p4 @editor +found found:2026-07-30 est:30m vis:12 SEEN CodeMirror builds its height map from getBoundingClientRect which excludes margin so a margin on a widget is space the editor does not know exists and every caret position after it resolves to the wrong row while the document looks perfectly correct THE TABLE has an invariant test now but nothing stops the next widget being written the same way DONE tests/e2e/editor-geometry.spec.ts states the rule from both ends THE CAUSE nothing inside cm-content claims vertical space with a margin and THE CONSEQUENCE every block widget occupies exactly its own box since a widget can keep the margin rule and still take space the height map lacks through a neighbour's margin or a collapsing gap A WIDGET IS a child of the content that is not a cm-line rather than a list of classes so the next one is covered without anyone remembering to add it MEASURED NOT GREPPED which departs from what this note asked for and the reason is worth keeping the token guard is static because a dangling var cannot be seen at runtime without landing on the element that uses it whereas this effect is visible on every element at once AND a static check would need a list of which selectors count as inside the content because .md-editor itself carries margin-block and THAT ONE IS CORRECT it is the focus gutter which lives on the column precisely because a container's padding is a floor under its own height A GUARD WITH AN EXCEPTION LIST IS A SECOND WEAKER STATEMENT OF THE RULE SWEEPS THE DOCUMENT because CodeMirror virtualises and a tall viewport does not change that so a sweep trusting one screen would report a clean surface having looked at a third of it JUSTIFIED against the iterate-the-whole-document rule since the claim IS about the whole document and it costs about two seconds A THIRD TEST IS A CONTROL both assertions are nothing was wrong which is trivially true of a sweep that looked at nothing so it asserts the table was actually reached PROVED by putting the original bug back margin-block on the editor table and watching both guards go red while the control stayed green THE TABLE REGRESSION TEST STAYS in editor-caret.spec.ts with a pointer here since it is the test for the reported defect and this is the class
x 2026-08-01 (B) 2026-08-01 CodeMirror runs every queued measure READ and then every queued WRITE so a write that scrolls must be coalesced +p4 @editor +found found:2026-08-01 est:20m vis:12 SEEN typewriter mode queued one requestMeasure per keystroke and each write applied a scroll delta computed from a rect measured before the previous write had scrolled so the corrections compounded and the caret bounced 400 204 624 44 16 812 down the window FIXED with a single outstanding request WORTH KNOWING AS A CLASS any measure whose write changes layout has this shape and focus.ts already queues one per selection change for the painted row which happens to be safe because it writes a decoration and not a scroll CHECK the anchor remeasure too before the next one is written AUDITED ALL SEVEN measures in the product and only TWO of them scroll typewriter pin and openOnAnchor the reader's scroll focus and the editor's remeasure and measureRow all write a class or a decoration so a stale rect costs nothing and each is coalesced anyway THE ANCHOR REMEASURE IS SAFE TWICE OVER it has a queued flag and its write does not scroll BUT openOnAnchor WAS SAFE ONLY BECAUSE IT IS CALLED EXACTLY ONCE which is the kind of safety that stops being true when someone adds a caller and setText landing for 6.3's external reload makes re-opening on the anchor a plausible next one DONE measure.ts holds the guarantee and both scrolling measures go through it rather than each remembering the rule since nothing about requestMeasure suggests that calling it twice in a frame differs from calling it once THE REAL FINDING IS THE TEST GAP the guard shipped 2026-08-01 with six passing typewriter tests and NOT ONE went red when the coalescing was removed the next day BECAUSE every one of them polls until the caret settles and the oscillation DOES settle once typing stops so the whole defect lives in the transient and an assertion made after things come to rest is blind to it RULE when a bug is only visible while something is happening the test has to read WHILE it happens not after ADDED the caret never wanders while you type which samples after each keystroke with no poll and PROVED by removing the coalescing and watching it report 168 412 356 384 384 356 412 356 412 against a two-row band MEASURED ALSO that with the guard the caret reads 28px low for one frame after Enter which is one row and is filed separately
x 2026-08-01 (B) 2026-08-01 A full suite run once reported 11 passed in 1.3 minutes and the cause is unknown +p4 @editor +found found:2026-08-01 est:20m vis:12 SEEN a run started immediately after a cp restored md.css reported 11 passed in 1.3 minutes while the four runs around it were 363 passed in 19 seconds THE TITLE OF THIS TASK WAS A GUESS and it said vite was still reloading the stylesheet as though that were established WHICH IT WAS NOT and this is the exact rule I keep citing at other people a filed suspect is written as the QUESTION it answers not as the answer SIX ATTEMPTS TO REPRODUCE all failed three touch-then-run-immediately cycles all 365 passed at 19.2 seconds plus the three runs that first revealed the anomaly ALSO MEASURED that no dev server persists between runs at all playwright starts one per run and shuts it down after so reuseExistingServer never fires locally and the warm-server-mid-rebuild story cannot be what happened NO GUARD BUILT because building one for an unreproduced cause is the same mistake in a more expensive form and AGENTS.md says the same thing about optimising without a profile WHAT SHIPPED INSTEAD is the lesson that IS supported by the evidence a catastrophic result is the one you re-run before believing since two or three failures are evidence and two hundred are a claim about the harness which is the thing you were about to trust ADDED to the Verify section of both the live session command and the portable way-of-working template together with say so and leave the cause open when it does not reproduce LEFT OPEN deliberately if it happens again the note is here and the reproduction attempts are recorded so the next person starts from what has already been ruled out
x 2026-08-01 (B) 2026-08-01 A tool edit to a task line must be an exact-match replace never string surgery +p4 @reset +found found:2026-08-01 est:20m vis:12 SEEN a python script doing index arithmetic on docs/todo.txt truncated it from 102 lines to 37 and the same class of mistake clobbered a task line's subject on 2026-07-31 RECOVERED both times by git checkout because the file was committed but the second one was only luck the archive had just landed THE FILE IS THE PLAN AND THE SESSION LOG so it is the one file where a careless write costs work that cannot be rebuilt from the code RULE use an editor that fails when the anchor does not match rather than a script that computes offsets and silently writes whatever it computed DONE AS A GUARD RATHER THAN ONLY A RULE tests/unit/task-format.test.ts checks both task lists against the grammar in way-of-working/task-format.md and runs in npm test which every session runs before it commits MEASURED THE INVARIANTS FIRST rather than inventing them exactly one priority and exactly one est: hold on all 176 lines across both files while +pN does NOT because prose legitimately mentions open +p3 tasks and vis: is absent from twelve archive lines so neither is enforceable BOTH INCIDENTS REPRODUCED against a copy and both are caught the clobber trips the one-priority rule and names the file and line number and a truncation to a fragment trips the control and the checkpoint check together WHAT STILL SLIPS THROUGH is a partial truncation leaving more than twenty well-formed lines and one checkpoint since no test can know how long the file ought to be and that is written in the file rather than implied CHECKPOINTS ARE CHECKED FOR EXISTENCE NOT COUNT because the human adds and moves them and a number would fail every time they did RULE ALSO WRITTEN where the edit actually happens which is step 6 of the session command in both the live copy and the portable template rather than in a document nobody opens while editing
x 2026-08-01 (B) 2026-07-30 Teach the round trip measurement in way-of-working position to coordinates to position +p4 @reset +found found:2026-07-30 est:20m vis:12 SEEN it found in one pass what three probes built on text matching and dom line indexing got confidently wrong including one that produced a filed task with a wrong root cause REACH FOR IT FIRST whenever a position and a coordinate might disagree ALSO 2026-07-31 a browser test is not always the honest one Playwright synthesises key z for Alt+z from a US layout so the whole e2e suite would have gone green while every alt chord was dead on macOS platform composition belongs in a unit test that hands the matcher the event the platform actually delivers ALSO 2026-07-31 teach that a filed suspect is written as the question it answers not as the answer the status strip task carried my guess that clearStatus dropped the scrollTop and it was wrong in every part not the removal not the strip not the fade and it sat in the task line for a day looking like a finding four control measurements located it in one pass at a minute each ALSO 2026-07-31 teach that a bisect narrows to a file and reading finds the line the fence bug was found by reading after a bisect had already cleared the wrong suspect and the comment above the faulty line described the failure it caused two of this week's diagnoses went faster once the probing stopped DONE docs/way-of-working/diagnosis.md holds all four as one document since every one of them is about the same act finding out what is true rather than building anything and each carries the incident that paid for it MEASURE THE ROUND TRIP distrust a harness that supplies the very thing you are testing file a suspect as a QUESTION and read the file once a bisect has named it PORTABLE ON PURPOSE the folder's own claim is that nothing in it links out so the Playwright and macOS specifics are told as the incident and the rule above them is stated without them LINKED from the README table and from step 4 of the session command in both the live copy and the template because a reference nobody opens while working is the exact failure I criticised the day before NOT COUNTED against the folder's four markdown files line which means the four COMMANDS and is still true
x 2026-08-01 (B) 2026-08-01 The checkpoint exemption in the task list guard skips any line that merely mentions the word +p4 @editor +found found:2026-08-01 est:20m vis:12 SEEN tests/unit/task-format.test.ts calls a line a checkpoint when it CONTAINS the word anywhere so a task line whose PROSE mentions checkpoints is excused from the grammar the guard exists to enforce and docs/todo.txt already has one written the same day the guard shipped while todo-archive.txt has another NEITHER HIDES A DEFECT TODAY both carry exactly one priority and one est: and would pass BUT a guard whose hole widens every time someone writes about checkpoints is worth about as much as no guard AND IT WAS FOUND BY ACCIDENT while counting lines rather than by the guard noticing anything FIX a predicate anchored at the start of the line which matches all six real checkpoints in the plan plus the completed one in the archive and excludes both prose mentions VERIFIED before filing DONE the predicate is anchored and allows the two real prefixes x date for a checkpoint already passed and priority plus date for one carrying a full task prefix the way task-format.md's own example does THE FINDING WAS WRONG ABOUT THE ARCHIVE it called that line a second prose mention and it is not it is a GENUINE completed checkpoint with a full task prefix so it stays exempt and correctly so which is why the anchored predicate had to allow that shape rather than simply demanding the line START with the word RED FIRST the exemption is now tested directly with five shapes that must be exempt and two that must not and three of those went red before the change NET EFFECT one more line checked in the plan and none lost in the archive measured rather than assumed WHY TEST THE PREDICATE AT ALL because every rule in that file is SKIPPED for a line it returns true for so a predicate that is too generous does not fail loudly it quietly stops checking which is the only failure mode a guard can have that is worse than being absent RED-FIRST WAS ALMOST MISSED I wrote the fix before the test and reverted it to do this properly which is worth recording because the temptation is strongest on a change this small
x 2026-07-30 (B) 2026-08-01 DECIDE whether a paragraph that introduces a code block focuses with it or whether section granularity is the answer +p2 @design +fb fb:2026-07-30 est:20m vis:4.1 THE REMAINING HALF of the paragraph-and-fence task above which measured that neither granularity matches the want paragraph is too narrow it takes the prose alone and section is too wide it takes the whole heading and everything under it YOUR CALL BECAUSE granularity is 7.6's own vocabulary so pairing two adjacent blocks at paragraph granularity rewrites that sentence rather than fixing an implementation OPTIONS leave 7.6 alone and reach the want through section granularity which needs the settings surface below or add a lead-in rule where a paragraph immediately followed by a fence focuses with it or a fourth granularity WANTS A COMPARE the difference is entirely a matter of what it looks like on screen ANSWERED 2026-08-01 by a second report of the same line this time with a blocking bang if its highlighted for focus the code block right below it should be highlighted for focus SO THEY PAIR and the work is now a blocking task above rather than a question here WHAT IS STILL OPEN is which rule expresses it a lead-in pairing or a fourth granularity since section granularity takes the whole heading and everything under it CLOSED 2026-07-30 the lead-in pairing shipped and the whole argument is written on the task line above rather than repeated here NO COMPARE WAS RENDERED which departs from the convention this line asked for and the reason is that the third option had already been eliminated in prose a fourth granularity cannot be chosen while the settings surface is unbuilt so there were two candidates left and they differ in where the rule is WRITTEN not in what the screen looks like

## 2026-07-31

x 2026-07-30 (A) 2026-07-30 Any scroll the surface makes on its own eases rather than jumping a row at a time +p2 @editor +fb fb:2026-07-30 est:45m vis:2 REPORTED as the line animation shifting up is not smooth enough and it should be a smoother scroll up anytime text scrolls with iA Writer named as the example SCOPE is every scroll the surface initiates typewriter recentring the focus block jump the caret-into-view after Enter not only the one moment after a new line since the report says ANYTIME text scrolls NOT the user's own wheel or trackpad which is already the OS easing and must not be second-guessed 2 SAYS motion is either immediate or eased never janky and a row-sized jump with no easing is neither SEE the closed one-frame caret task below which was a different complaint about the same moment and was answered no HALF SHIPPED 2026-07-30 the focus block jump eases and editor-scroll-easing.spec.ts samples per frame and asserts the surface passes through the space between the two positions rather than cutting MECHANISM scrollBoxTo takes a ScrollBehavior defaulting to instant and scrollBlockToAnchor passes smooth TRIED CSS FIRST scroll-behavior smooth on md-surface which is one line and is what this repo prefers for motion AND IT TURNED 28 SPECS RED because the property covers every programmatic scroll and only some of them are motion the clearest being 4.1's a document OPENS with its first block on the anchor which is a position not a journey so easing it means it opens somewhere else and slides WHAT IS LEFT is the typewriter pin which is the moment the report was actually looking at MEASURED IT BOTH WAYS one Enter eased perfectly 28 27 23 13 7 4 3 1 1 0 back to the midpoint but nine lines typed in a row went 28 55 83 110 137 164 191 218 245 walking a row further down the window per line and never catching up and twelve ArrowDowns left it 384px low and still wrong five seconds later BECAUSE a keystroke arrives long before the browser's easing finishes and each one retargets an animation already behind WHICH BREAKS 6.1's the caret line holds its vertical position while the document moves under it so it was reverted and the reasoning is in typewriter.ts beside the call NEEDS an easing short enough to always finish inside a keystroke which the browser's duration is not and does not expose so that is a JS animation on this one scroll and it is the remaining scope of this line CLOSED 2026-07-30 the typewriter pin now eases through a nudge THE DESIGN IS THE GIVE-UP rather than the easing curve a nudge travels at most one and a half rows and PLACES rather than eases past that so the ordinary case a new line or a caret moved by one glides in about 140ms per row and a burst of text arriving faster than the ease resolves cuts to the midpoint instead of falling behind WHICH IS WHY the caret never wanders while you type still passes it is the one assertion in that file that could see the browser-smooth regression and it reads the caret with no poll and no settle THREE NAMED MOTIONS NOW instant for a position smooth for a journey nudge for holding a pinned line and the type is ScrollMotion in reader/focus.ts so each caller says which it means ALSO REMOVED the scroll-behavior auto rule added to reset.css earlier the same day it could not reach either mechanism since an explicit behavior passed to scrollTo overrides the CSS by spec and a JS animation on scrollTop is invisible to a stylesheet so reduced motion is asked directly in focus.ts and the empty space in reset.css says why LEFT the bottom-of-document recentring which is the next line in this band
x 2026-07-30 (A) 2026-07-30 At the bottom of the document scrolling down should ease the caret's position back to the anchor rather than leaving it where it lands +p2 @editor +fb fb:2026-07-30 est:45m vis:4.1 REPORTED as when you reach the bottom of a page and scroll down it shouldn't just scroll and move down from there it should smoothly return the caret back to center by scrolling that doc position to the center of the page again NOTE 4.1 puts the anchor above the middle roughly a third of the way down rather than at the geometric centre and that ratio is the decision so centre in the report means the anchor TENSION with 4.1's once the caret is in the document scrolling for context leaves focus where it is so this is about where the SURFACE settles not about moving focus TAKE IT WITH the easing task above since both are the same mechanism MEASURED 2026-07-30 and the report is exactly right walking ArrowDown down a fresh document in an 800px window the caret climbs 96 152 208 through 768 and then from press 24 onward it sits at 745 to 773 for EVERY remaining press while scrollTop walks 409 437 465 through 1131 so the caret is welded to the bottom edge and the document slides past underneath THAT IS CODEMIRROR's scroll-into-view working as designed it puts the caret BARELY on screen ATTEMPTED 2026-07-30 and REVERTED an edge-return in the focus plugin firing when the caret comes within two rows of either edge declining for typewriter mode for a pointer selection and when no caret is placed yet TWO THINGS WERE LEARNED AND BOTH ARE WORTH THE SESSION FIRST the block jump and the return both want to put something on the anchor and they disagree about WHAT the jump means the block and the return means the caret's row so the caret placement won and left the landed block 97.5px off the anchor since a paragraph's centre is not its first line FIXED by giving jumpFocusBlock its own userEvent select.blockjump which isUserEvent select still matches so the return can stand down for it SECOND AND THIS IS THE BLOCKER a browser-managed smooth scroll DOES NOT SURVIVE HERE it is one animation the browser owns and any direct scrollTop write cancels it and CodeMirror writes exactly that in its own measure phase for the same selection change that triggered the return PROBED with a console forwarder the edge test returned true on every press at the bottom and the scroll simply never happened WHICH ALSO EXPLAINS why the typewriter pin works it is instant so it wins the last write NUDGE SURVIVES that because it writes scrollTop itself on every frame BUT it places rather than eases past one and a half rows and this distance always is so nudge gives the behaviour and not the smoothly NEXT TIME either give nudgeTo a caller-supplied allowance and a fixed duration so a long return can ease over about 200ms regardless of distance or accept an instant return and reword the smoothly NOTE the red-first spec was written and deleted with the revert its shape was walk down until the caret passes 85 percent of the window then ONE more press and poll the caret back to within two rows of the anchor which is a good shape and worth rewriting rather than reinventing CLOSED 2026-07-30 by the first branch of that fork which needed no spec change scrollBoxTo gained a fourth named motion return which eases over a FIXED 220ms whatever the distance and never gives up where nudge gives up past one and a half rows MEASURED settling 736 509 321 255 239 against an anchor at 267 so it arrives inside about 200ms THREE THINGS THE SESSION PAID FOR FIRST the two-row edge band one row is the distance CodeMirror leaves so a one-row band caught only alternate presses 770 then 798 against a 772 threshold and the return never happened SECOND the ease MUST YIELD to anyone else who moves the surface 7.6 says manual scrolling is always allowed and an animation that keeps writing scrollTop for its full duration overwrites a trackpad flick arriving halfway through CAUGHT BY editor-status's a command does not drag the document back to the caret which sets scrollTop to 1500 to read ahead and found the surface back at the top so easeScrollTo now compares against what it last wrote and abandons when the number is not its own THIRD the DOM selection rect CANNOT MEASURE A CARET ON A BLANK LINE there is no cm-cursor element on this surface so a spec has only a collapsed Range and that reports no rect at all on an empty line which is most of a markdown document FIXED by an editor caretY through coordsAtPos exposed on the fixture hook the same way anchorY is asked rather than restated ALSO the walk-down helper waits two frames per press without it it read the caret in the same breath as the press and passed alone every time while failing every full run with the caret finishing at 868 in an 800px window NOTE two of three full runs green the third failed editor-scroll-easing which passes alone and is the known frame-sampling family see the focus specs task
x 2026-07-30 (B) 2026-07-30 The unsaved-close confirmation is not a prominent enough signal that a choice has to be made +p2 @shell +fb fb:2026-07-30 est:30m vis:6.3 REPORTED as it should probably be a more prominant signal that they have to confirm or click cancel on FOLLOWS the confirm-exit-without-save task that shipped 2026-08-01 so the dialog exists and this is about how it reads TENSION with 2's chrome-free and 6.3's unsaved state is visible without adding chrome so more prominent has to mean more legible rather than more chrome CLOSED 2026-07-30 the prompt moved from the ten second strip to the persistent notice and now names what the second press costs THE ARGUMENT IS NOT TASTE the arming behind that prompt DOES NOT EXPIRE askedOnce clears on a save and on nothing else so ten seconds after the strip left the arming was still live and the next close request closed the window over work that was never written with the reader having been told once briefly and no way to check which state they were in SO THIS IS THE FOURTH 6.3 MESSAGE on the persistent notice for audit finding M3's reason taken one step further evidence that expires is not evidence and here the thing it is evidence OF outlives it THE ROUTING RULE in index.ts said the close refusal was informational alongside reloaded and that line is now redrawn THE TEST IS NOT whether the message tells you to do something it is whether the state it describes outlives it WORDING also changed since the report asked for a signal they have to confirm or cancel and there are no buttons to click so both ways out are in the sentence and so is the consequence save with cmd+s to keep them or ask to close again to lose them FOUND WHILE HERE and filed in agent-findings the block jump landing spec is consistently red IN ISOLATION and green in a full run at 97.526 which is the caret's row winning over the block's centre and it arrived with the edge return the day before
x 2026-07-30 (B) 2026-07-30 The block jump's landing spec is consistently red in isolation and green in a full run +p2 @editor +found found:2026-07-30 est:45m vis:4.1 SEEN editor-block-jump.spec.ts:258 the block it lands on sits on the reading anchor received 97.526 against a bound of 2 and timed out the 5s poll THAT NUMBER IS A SIGNATURE already diagnosed the same day the caret's ROW on the anchor rather than the BLOCK's centre because a paragraph's centre is not its first line ARRIVED WITH the edge return shipped 2026-07-30 and was invisible because every check that session was a full run REVERSED LOAD DEPENDENCE green under parallel load and red alone is the same shape editor-focus-line turned out to have and that one was not a flake at all it was consistently red in isolation TWO THINGS TRIED AND BOTH REVERTED FOR FIXING NOTHING the select.blockjump user event already guards the jump's own dispatch and the return does decline for it so something LATER is claiming the scroll and retiring the browser's smooth so the surface has one animation mechanism left the number identical NEXT measure rather than reason log every putOnAnchor request and every returnFromEdge decision across one Alt+ArrowDown with a page.on console forwarder since Playwright does not print browser console by default SUSPECT an update after the jump carrying selectionSet since measureRow dispatches an empty transaction in a microtask and the scroll itself raises geometryChanged FILED AT THE FRONT OF THIS BAND rather than after the table task because it is a regression in shipped behaviour and the suite cannot be trusted to catch the next one while it is red CLOSED 2026-07-30 and BOTH EARLIER GUESSES WERE WRONG nothing was claiming the scroll and no animation was being cancelled INSTRUMENTED putOnAnchor at request and at write and returnFromEdge at entry with a page.on console forwarder and the log said it in one line requested smooth appeared and writes smooth never did THE CAUSE IS THE COALESCING measure.ts holds ONE outstanding scrolling request and it kept the FIRST and dropped the rest so Alt+ArrowDown pressed while an edge return was still pending had its request thrown away the return then DECLINED because the caret was not at an edge after all and nothing scrolled at all leaving the block 97.5px off which is exactly the delta the jump asked for and never got THE OLD REASONING was that the single read that survives sees the final position which is true of the LAST request just as much as the first and is only equivalent while every caller wants the same thing two callers here want different things the return means the caret's row and the jump means the block's centre FIX newest wins which is the same rule the persistent notice already uses the newer message is the truer one GUARDED by tests/unit/scrolling-measure.test.ts against a fake view that separates the read phase from the write phase since the whole claim is about which callback survives WHY IT WAS LOAD DEPENDENT under parallel load the return resolved before the press arrived so there was nothing to drop THE MEASURE-DONT-REASON RULE PAID FOR ITSELF the two previous sessions guessed a claimant and then a cancelled animation and reverted both the probe found it in one pass ALSO a test bug worth remembering the queue depth assertion was written against a spread of the fake view and spreading a getter copies the number it held at spread time which is zero always and reads as a passing assertion about a queue nobody looked at
x 2026-07-30 (B) 2026-07-30 The caret cannot enter a rendered table so it is never the focus target and the block jump steps over it +p2 @editor +fb fb:2026-07-30 est:45m vis:6.1 REPORTED as tables still render weird and the caret never goes into them so they are never focused and it often skips them when going through doc THREE COMPLAINTS IN ONE LINE and only the last two are this task the rendering one is the table editing task in the phase 4 band which has now been reported four times SUSPECT the rendered table is a block widget so there are no cm-line positions inside it to place a caret on and jumpFocusBlock walks source lines looking for text that is not blank which a table's source lines are so it should stop there MEASURE THAT FIRST rather than believing it since skipping and landing-then-being-pushed-out look identical from the outside SAME ROOT as the closed tables never dim finding the widget sits outside what the surface does to every other construct SEE ALSO the table editing task which is the same widget seen from the other end CLOSED 2026-07-30 and MEASURING FIRST WAS RIGHT the report splits in two and half of it was already working OPTION+ARROW WAS FINE the block jump lands on the table's first source line and focus paints it because jumpFocusBlock walks SOURCE lines and a table's are not blank so the suspect on this line was wrong PLAIN ARROWDOWN WAS NOT measured 83 straight to 91 the whole seven line table in one press because 6.1 renders a table as a block widget and a replaced range has no positions inside it so vertical motion has nowhere to land and CodeMirror correctly moves to the next place that does CORRECT FOR A CODE EDITOR wrong for this one since 4.1 makes the caret the focus target and a construct the caret can never occupy can never be read at full contrast while you walk the document FIX one stop at the range's start through a tableStop command per arrow direction in table.ts CHESTERTON'S FENCE the atomicRanges line right above it was added for an earlier report up from below a table landed above it no matter how far below and that is still true and still needed the atomic range stops the caret landing at an offset with no visual position and the stop adds the boundary it left out so both notes now sit together DECLINES rather than claims which is continuation.ts's contract and not motion.ts's since several commands want the arrows and each claims a different case and reimplementing vertical motion over wrapped rows is the library's job DECLINES FROM INSIDE THE RANGE TOO which is what keeps the stop from being a trap PREC.HIGH rather than reordering editor.ts because the table extension is registered AFTER defaultKeymap and has to be a block decoration only a StateField may provide so splitting the keymap out to sit earlier would put one construct's behaviour in two places TWO SPEC BUGS OF MY OWN worth remembering the focus assertion filtered content children by querySelector table and TableWidget.toDOM returns the table ITSELF so the host IS the table and a descendant filter found nothing and read as the table was never focused when it never looked at a table at all AND the arrow up case started two lines below the table so one press should land one line below and the spec was asking the caret to skip a line to prove it does not skip a table NOT DONE the rendering half of the report tables still render weird which stays with the phase 4 table editing task NOTE two of three full runs green the third failed focus-block-boundary which passes alone and is the known parallel load family
x 2026-07-30 (B) 2026-08-01 The filesystem IPC surface is unscoped so any path can be read or written +p4 @shell +found found:2026-08-01 est:60m vis:9 audit:M2 SEE the audit BEST DONE with or just before the phase 3 workspace since that is when a root exists to scope to CLOSED 2026-07-30 and THE PHASE 3 NOTE WAS A PREFERENCE NOT A BLOCKER the audit's own recommendation scopes to the LAUNCH path which already exists and it calls the work cheap now one state struct one check so waiting three checkpoints with an unscoped read and write surface was the wrong reading of its own line WHAT SHIPPED fs Scope holds the launch folder a file scopes to its parent a folder to itself within_scope canonicalizes both sides and refuses anything that does not start with the root and all three commands read write and stamp go through it THE SCOPE IS READ IN run FROM cli launch_request rather than from the frontend's call to it because a boundary the untrusted side can decline to establish is not a boundary FILE_STAMP REPORTS NONE for an out of scope path rather than erroring since its contract is already that a missing file is a state not a failure and it is asked on every window focus so a loud refusal would put a scope error on the 7.3 notice every time the window came back MY OWN IMPLEMENTATION HAD A HOLE AND THE TEST CAUGHT IT canonicalizing only the PARENT and rejoining the file name let a symlink at an in scope name point anywhere since its parent is inside and its target is not so the whole path is canonicalized when something exists at it and the rejoined path is the fallback for zd md new-file.md which names something not there yet ALSO SPLIT stamp_of out of the command because a tauri State cannot be built outside a running app so a test going through the command would be testing the framework's injection rather than the answer L5 AND L6 ARE ANSWERED by the same canonicalization dot dot cannot survive into a comparison and a write follows a symlink to its target instead of replacing the link 34 cargo tests green clippy clean 288 unit 393 e2e
x 2026-07-30 (B) 2026-08-01 In typewriter mode the caret reads one row low for a frame after Enter +p2 @editor +found found:2026-08-01 est:20m vis:6.1 MEASURED PER FRAME 2026-08-01 and it is exactly one painted frame 400 then 428 then 400 400 400 for the rest an ordinary character never shows it so it is the NEW LINE specifically NOT A LAG IN THIS CODE the cause is architectural CodeMirror updates the DOM synchronously in the key handler and applies scrolling on the NEXT animation frame so the new line is painted before anything can compensate PROVED by routing the scroll through the library's own EditorView.scrollIntoView y center in a transactionExtender which lands inside the update rather than in a later measure AND IT PRODUCES THE IDENTICAL 28px FRAME so the frame is CodeMirror's and neither mechanism closes it from here THAT ATTEMPT WAS REVERTED since it fixed nothing and a mechanism change with no benefit is only risk STILL OPEN 2 says nothing flashes jumps or reflows while you work and does not grade that on duration so this is a real if small departure BOUNDED MEANWHILE by a per-frame test asserting at most ONE frame and at most ONE row since two frames would be a visible judder and two rows would mean the compensation had stopped tracking OPTIONS live with it or drive the scroll outside CodeMirror by moving md-surface's scrollTop in the keydown handler before the frame is painted which is a bigger change than it sounds because every other scroll goes through the measure phase LOOK AT IT holding Enter in typewriter mode and say whether it reads as a judder because that decides whether the bigger change is worth making ANSWERED 2026-07-30 the caret does not judder on new lines in typewriter SO THE BIGGER CHANGE IS NOT WORTH MAKING and md-surface's scrollTop stays out of the keydown handler CLOSED as a decision rather than as a fix the one frame is still there and the per-frame test still bounds it at one frame and one row which is what keeps this from silently becoming two NOTE the same report continued that the scroll ITSELF is not smooth enough which is a different complaint about the same moment and is now the blocking easing task at the top of this band

## 2026-07-31

x 2026-07-31 (A) 2026-07-31 Returning the caret from the bottom edge to the focal point eases too quickly +p2 @editor +fb fb:2026-07-31 est:30m vis:4.1 REPORTED as bottom of screen caret warp to focal point to scroll page easing is too fast REGRESSION of the closed bottom-of-document return which fixed the landing but set a 220ms duration that now reads as a warp
x 2026-07-31 (A) 2026-07-31 The editor takes focus on launch and briefly teaches opt+down-arrow focus navigation +p2 @editor +fb fb:2026-07-31 est:45m vis:7.1 REPORTED as when the app starts auto focus the editor so the caret is placed and then show at top right of editor for about 5 seconds a tooltip that says Use opt+down-arrow to shift your focus while reading
x 2026-07-31 (B) 2026-07-31 Option-arrow focus jumps are unreliable in typewriter mode and hop instead of easing to the focal point +p2 @editor +fb fb:2026-07-31 est:45m vis:4.1 REPORTED as option+arrow key is unreliable and jump and in typewriter mode it hops around the screen while normal mode works as expected
x 2026-07-31 (B) 2026-07-31 Holding Enter in typewriter mode judders the full document instead of scrolling smoothly +p2 @editor +fb fb:2026-07-31 est:30m vis:6.1 REPORTED as a single enter seems smooth but holding enter and key repeat causes the full document to judder
x 2026-07-31 (B) 2026-07-31 Cmd+w closes an unsaved window without asking for confirmation +p2 @shell +fb fb:2026-07-31 est:30m vis:6.3 REPORTED as cmd+w just closed the window and did not ask for a confirm to quit without save REGRESSION of the closed confirm-exit-without-save task

## 2026-08-01

x 2026-08-01 (A) 2026-08-01 Cmd+w needs a visible unsaved-changes dialog with explicit Cancel and Close buttons +p2 @shell +fb fb:2026-08-01 est:45m vis:6.3 REPORTED after the closed unsaved-window task shipped the close is now prevented but there is still no visual confirmation to act on THIS REPLACES the rejected second-press confirmation interaction with the standard explicit choice already requested on 2026-07-31
x 2026-08-01 (A) 2026-07-31 Refile the repository into packages/app packages/tauri and packages/scripts with tests and assets owned by their package and remove unused loose entry points +p2 @reset +fb fb:2026-07-31 fb:2026-08-01 est:60m vis:12 SCOPE includes src src-tauri scripts tests index.html dev html files ruff configuration fonts and other root assets FIRST inventory which files are real inputs because the request explicitly removes only unused entry points PROMOTED 2026-08-01 from +p4 and moved to the front of the live band by blocking feedback do the folder reorganization I mentioned before
x 2026-07-31 (A) 2026-07-31 Opening a document again leaves the editor unfocused with no caret +p2 @editor +fb fb:2026-07-31 est:30m vis:6.1 REGRESSION of the closed launch-focus task which shipped 2026-07-31 and was reported again the same day
x 2026-07-31 (A) 2026-07-31 Option-arrow focus jumps should reuse the slower easing of the bottom-edge return +p2 @editor +fb fb:2026-07-31 est:45m vis:4.1 REGRESSION of the closed typewriter-mode block-jump easing task which shipped 2026-07-31
x 2026-07-31 (A) 2026-07-31 Holding Enter or ArrowDown or ArrowUp in typewriter mode still judders instead of scrolling smoothly +p2 @editor +fb fb:2026-07-31 est:45m vis:6.1 REGRESSION of the closed repeated-Enter judder task which shipped 2026-07-31 and now includes held vertical navigation
x 2026-07-31 (A) 2026-07-31 Cmd+w can close an unsaved window on the second press without showing a confirmation to click +p2 @shell +fb fb:2026-07-31 est:45m vis:6.3 REGRESSION of the closed unsaved-window confirmation task which shipped 2026-07-31 and deliberately used a second close request as confirmation THIS REPORT REJECTS that interaction and asks for a standard explicit confirm-or-cancel choice

## 2026-08-03

x 2026-08-01 (B) 2026-08-01 Occasional Option-arrow focus jumps catch and release with a performance judder +p2 @editor +fb fb:2026-08-01 est:45m vis:6.1 REPORTED after the focus-block easing task shipped and distinct from the deliberately slower easing because it happens only every once in a while MEASURE the round trip before changing motion
x 2026-08-03 (A) 2026-08-03 Make the zdloop agent stream mouse-scrollable so a summary does not trap earlier output +p4 @reset +fb fb:2026-08-03 est:30m vis:12
x 2026-08-03 (A) 2026-08-03 Ask for the Codex model effort level and fast mode when zdloop starts +p4 @reset +fb fb:2026-08-03 est:45m vis:12
x 2026-08-03 (B) 2026-07-31 A rule for a construct that appears on both surfaces goes in md.css from the start +p4 @editor +found found:2026-07-31 est:20m vis:12 SEEN reading.css is never loaded on the editor page and md.css says so in three separate comments yet every construct that has needed sharing so far tables syntax categories links images and inline code was written in reading.css first and moved later the pattern is reliable enough to invert WORTH a note in md.css itself so the next construct starts in the right file FOLLOWED ONCE 2026-08-01 emphasis went straight into md.css over all three selector families and the reasoning is written beside the rule so it is now demonstrated rather than only asserted WHAT IS LEFT is the note at the top of md.css that says it before the next construct rather than after
x 2026-08-03 (B) 2026-08-01 A token that no rule consumes is a design decision that was recorded and never applied +p4 @design +found found:2026-08-01 est:20m vis:12 SEEN --type-prose-emphasis-family style and weight sat in tokens.css spent by nothing until 2026-08-01 and --text-link did exactly the same until links were built so this is twice THE TOKEN GUARD RUNS THE OTHER WAY token-references.test.ts proves every var() resolves to a defined token and nothing proves a defined token reaches a rule COST the editor read emphasis as plain prose while the reader italicised it and the divergence was invisible because both halves looked complete on their own WORTH a companion check listing tokens no stylesheet mentions NOTE some are legitimately unspent for now so it is a list to read not a test to fail
x 2026-08-03 (B) 2026-08-01 No bold-italic face ships so strong containing emphasis synthesises a slant +p4 @design +found found:2026-08-01 est:20m vis:8 SEEN fonts.css declares Quattro regular Quattro italic and Quattro Bold upright and there is no bold italic so **strong _and italic_** falls back to a synthesized oblique on the bold face WHICH 5.2 FORBIDS in as many words the drawn italic face never a slant applied to the upright DECIDE whether to ship the fourth face or to state that the combination resolves to bold alone SMALL but it is the one place the emphasis role cannot keep its own promise
x 2026-08-03 (B) 2026-07-29 Move the markdown to DOM renderer out of reader/ before the render path retires the editor table widget depends on it +p2 @editor +found found:2026-07-29 est:20m vis:6.1 SEEN renderInlineMarkdown in reader/markdown.ts is what the table widget calls for cell contents one parser is deliberate it is where html false and link protocol validation and remote image blocking are got right once BUT the file sits under reader/ and the retirement below will look like it can delete it move it to miniapps/md/markdown.ts first
x 2026-08-03 (B) 2026-07-29 Retire the render-only path once the editor reaches it dev/reader.html is the reference every editor decoration is measured against +p2 @editor sess:3.2 est:45m vis:6.1 SPLIT blocked on inline code tables images rules and setext in the editor BLOCKED 2026-07-29 verified the editor decorates only Blockquote FencedCode HeaderMark ListMark QuoteMark and 130 of 183 e2e tests sit in files that navigate to dev/reader.html RESEQUENCED 2026-07-29 behind the look tasks above
x 2026-08-03 (B) 2026-07-29 COMPARE tasks render a style choice side by side so a DECIDE is answered by looking teach the convention in way-of-working and open one for every DECIDE below +p2 @reset +fb fb:2026-07-29 est:45m vis:12
x 2026-08-03 (B) 2026-08-03 COMPARE Rust-only honest monospace and widened TypeScript and HTML highlighting on the same samples side by side +p2 @design @COMPARE est:30m vis:6.2
x 2026-08-03 (A) 2026-08-03 Move the sidebar work ahead of remaining cleanup so zd can become the daily driver sooner +p3 @reset +fb fb:2026-08-03 est:15m vis:5.1
x 2026-08-03 (A) 2026-08-01 zd md dot opens that folder and the sidebar shows its files +p3 @workspace +fb fb:2026-08-01 sess:2.1 est:20m vis:5.1 THE CLI HALF ALREADY SHIPS parse_args resolves a bare dot to the working directory and cli.rs has dot_opens_the_current_folder covering it so launch.path is already the folder WHAT IS MISSING is the other end nothing says what the sidebar is ROOTED at and the sidebar task beside this one describes how the tree looks and moves without saying where it starts SO THIS IS THE LINK between the launch request and the tree rather than a second sidebar NOTE the mini app currently treats launch.path as a document to read so opening a directory has to stop meaning read this file which is where 5.3's Home surface and this meet TAKE IT WITH the sidebar task since a root with no tree shows nothing
x 2026-08-03 (A) 2026-08-03 Make the sidebar file list a collapsible tree with a bounded resizable panel +p3 @workspace +fb fb:2026-08-03 est:45m vis:5.1
x 2026-08-03 (A) 2026-08-03 Review selected text with inline comments and one generated workspace feedback file +p3 @workspace +fb fb:2026-08-03 est:60m vis:5.1
x 2026-08-03 (A) 2026-08-03 Make the inline review comment composer a multiline textbox +p3 @workspace +fb fb:2026-08-03 est:20m vis:5.1
x 2026-08-03 (A) 2026-08-03 Delete an inline review comment from its document tag +p3 @workspace +fb fb:2026-08-03 est:30m vis:5.1
x 2026-08-03 (A) 2026-08-03 Sort file-tree folders first and files second with each group alphabetical +p3 @workspace +fb fb:2026-08-03 est:20m vis:5.1
x 2026-08-03 (A) 2026-08-03 Scroll the file tree horizontally when a path exceeds the panel width +p3 @workspace +fb fb:2026-08-03 est:20m vis:5.1
x 2026-08-03 (A) 2026-08-03 Start every file-tree folder collapsed by default +p3 @workspace +fb fb:2026-08-03 est:15m vis:5.1

## 2026-08-04

x 2026-08-03 (A) 2026-08-03 Make zdloop decisions identify exactly what comparison to inspect without requiring terminal scrollback +p4 @reset +fb fb:2026-08-03 est:30m vis:12
x 2026-08-03 (B) 2026-08-03 Show zdloop model thinking level and Fast mode state at the top of every TUI screen +p4 @reset +fb fb:2026-08-03 est:20m vis:12
x 2026-08-03 (B) 2026-08-03 Move the reading column left when word wrap is off so long lines use more of the viewport before overflowing +p2 @editor +fb fb:2026-08-03 est:30m ref:F03 vis:6.1
x 2026-08-03 (B) 2026-08-03 Make the chrome-free window draggable from its top area +p2 @shell +fb fb:2026-08-03 est:20m vis:2
x 2026-08-03 (B) 2026-08-01 DECIDE whether the highlighting inventory widens past Rust or 6.2 stops asking for language-appropriate highlighting +p2 @design @DECIDE +found found:2026-08-01 est:20m vis:6.2 THE TWO SENTENCES CONTRADICT vision 6.2 says a non-markdown file gets language-appropriate highlighting and DESIGN.md 5.2 fixes the inventory at Rust alone and adds that an absent or unknown language remains honest monospace text rather than receiving misleading language colour BUILT 5.2 on 2026-08-01 because it is the narrower claim and because widening is not an implementation detail COST a ts or html file today is honest monospace with no colour which is defensible and is also not what 6.2 promises OPTIONS widen the inventory which means new grammars new colour categories beyond 5.2's three and a design system change or reword 6.2 to promise honest monospace outside the inventory NOTE the grammars are cheap lang-javascript covers ts jsx and tsx and lang-html brings css and js with it so this is a design question and not a packaging one SEE the closed non-markdown task above for what already works
x 2026-08-03 (B) 2026-08-03 COMPARE clamped notation markers and a wider compact inset at the same narrow viewport side by side +p4 @design @COMPARE est:20m vis:4.2
x 2026-08-03 (B) 2026-07-30 At a narrow window the notation markers hang past the left edge of the screen and cannot be reached +p4 @design @DECIDE +found found:2026-07-30 est:30m vis:4.2 MEASURED 2026-07-30 at a 600px window the leftmost glyph on the editing surface sits 16px PAST the window frame so a heading's hash is half cut off and there is no scroll position that shows it since a surface cannot scroll left of zero THE HANG IS DELIBERATE the markers live outside the column which is 7.3's the reading column stays a clean straight line and the space they hang in is the 5.3 inset AT THE WIDE VALUE 64px there is room AT THE COMPACT 32px and the 24px floor there is not and nothing checks that the hang fits the inset it depends on FOUND while measuring the trailing edge for the word wrap margin task which is why the leading control in tests/e2e/editor-trailing-inset.spec.ts measures the COLUMN's own box rather than the leftmost glyph DECIDE clamp the hang to the inset that is actually available or raise the compact inset to fit the widest marker since DESIGN.md 5.3 fixes both numbers so either way is a spec change ANSWERED 2026-08-03 use the Widen compact inset on the compare markers
x 2026-08-03 (B) 2026-08-03 COMPARE paragraph and section focus on the same one-H1 document side by side +p1 @reader @COMPARE est:20m vis:4.1
x 2026-08-03 (B) 2026-07-29 DECIDE section granularity is inert when a document has one H1 change 7.6 or leave it +p1 @reader @DECIDE +found found:2026-07-29 est:20m vis:4.1
x 2026-08-03 (B) 2026-08-03 COMPARE h3 to h6 with stepped space alone and with stepped type side by side +p2 @design @COMPARE est:30m vis:4.2
x 2026-08-03 (B) 2026-07-30 DECIDE whether h3 to h6 need bigger size steps as well as stepped space 5.2 fixes them a pixel apart +p2 @design @DECIDE +found found:2026-07-29 est:20m vis:4.2
x 2026-08-03 (B) 2026-08-03 COMPARE inline code in headings with Mono Regular and a real Mono Bold face side by side +p2 @design @COMPARE est:20m vis:4.2
x 2026-08-03 (B) 2026-07-30 DECIDE whether inline code in a heading reading lighter is acceptable or needs a Mono Bold face in the 5.1 manifest +p2 @design @DECIDE +found found:2026-07-28 est:20m vis:4.2 DEFERRED by request until after the phase 2 checkpoint
x 2026-08-03 (B) 2026-08-03 COMPARE title-only and section-spaced mid-document h1 treatment side by side +p2 @design @COMPARE est:20m vis:4.2
x 2026-08-04 (B) 2026-07-29 DECIDE whether a mid-document h1 sits in space reading.css gives it none because h1 is the title +p2 @design @DECIDE +found found:2026-07-29 est:20m vis:4.2
x 2026-08-04 (B) 2026-07-28 Suppress the focus colour transition while a theme or dim setting is being applied +p4 @shell +found found:2026-07-28 sess:4.4 est:20m vis:6.3
x 2026-08-04 (B) 2026-07-28 Measure the widest ordered marker per list instead of a fixed 1.9em column only if 100+ item lists matter +p4 @reader +found found:2026-07-28 est:45m vis:4.2
x 2026-08-04 (B) 2026-07-29 Line granularity in the reader paints a shaped row too getClientRects gives one rect per row +p4 @reader +found found:2026-07-29 est:30m ref:F04 vis:4.1
x 2026-08-04 (B) 2026-08-03 Stop zdloop asking again when an open DECIDE task already carries an ANSWERED verdict +p4 @reset +found found:2026-08-03 est:30m vis:12
x 2026-08-04 (B) 2026-08-03 Make the check harness accept the intentionally absent CI workflow +p4 @reset +found found:2026-08-03 est:20m vis:12 SEEN npm run check has six baseline failures because packages/scripts/tests/unit/ci-workflow.test.ts requires .github/workflows/check.yml but the workflow is absent
x 2026-08-04 (B) 2026-08-03 Align typography specs with the 22 / 20 / 18 / 16 deep-heading scale +p4 @design +found found:2026-08-03 est:20m vis:4.2 SEEN the typography specs still assert the old 20 / 19 / 18 / 17 scale so npm run test:e2e has three baseline failures even though the editor heading coverage expects the production scale
x 2026-08-04 (B) 2026-08-03 Launch each comparison before asking for its decision and remove its dev artifact after the answer +p4 @reset +fb fb:2026-08-03 est:45m vis:12

## 2026-08-05

x 2026-08-05 (B) 2026-08-05 Diagnose the session-loop graceful-stop integration timeout only if it repeats +p4 @reset +found found:2026-08-05 est:30m vis:12 DONE the full suite reproduced the 10-second timeout twice while the exact scenario passed alone in about one second PRESERVED STAGE every named four-second wait had completed so only the final child exit was still pending ROOT CAUSES pauseAtSummary painted Summary ready before installing the c and q callback so a reader of the stdout pipe could answer inside a gap where a valid key was discarded then continuing repainted the consumed Summary ready phase during loop-state updates so a fast q could target a prompt that no longer had a handler FIX the callback now exists before the prompt is rendered and continue replaces the consumed phase before updating loop state while the regression writes q synchronously from the second Summary ready data event VERIFIED the focused regression and two full npm run check passes under parallel load with 377 active tests each
x 2026-08-05 (A) 2026-08-05 Audit and clean the repository for an open-source release +p5 @package est:60m vis:9
x 2026-08-05 (A) 2026-08-05 Back up and rewrite repository history into measurable public commits +p5 @package est:60m vis:9
x 2026-08-05 (A) 2026-08-05 Establish package.json as the 0.1.0 release version source +p5 @package est:60m vis:9
x 2026-08-05 (B) 2026-07-29 Design our own app icon +p5 @package +fb fb:2026-07-29 est:45m vis:9 MOVED 2026-07-30 out of the top of the file to sit above the bundle task that consumes the icon
x 2026-08-05 (C) 2026-07-28 macOS app bundle with icon and md file association +p5 @package sess:5.2 est:45m vis:9 DONE the Tauri bundle declares md and markdown as net.daringfireball.markdown documents with the app ranked as an alternate editor and the generated Info.plist proves the extensions UTI role and rank are present FINDER OPENS ARE BEHAVIOUR NOT JUST METADATA macOS RunEvent Opened file URLs enter one queued native launch state and the document accepts the new path only after its existing canSwitch guard says no unsaved work can be lost ACCEPTANCE MOVES THE FILESYSTEM SCOPE UNDER THE SAME LOCK so an untrusted webview cannot widen access by naming a path and a dirty old document never loses write access while the new request waits STARTUP RACE CLOSED the platform listener checks the durable pending request after subscribing so an event delivered before WebKit exists is replayed THE BUILD command is npm run package:macos and uses Tauri for the app plus a headless hdiutil DMG instead of Finder icon-position automation which timed out locally and would be brittle in CI THE COMPLETED APP is ad-hoc signed only when its existing signature does not verify so a future distribution signature is preserved VERIFIED npm run check 349 passed cargo test 38 passed cargo fmt bash syntax a real arm64 zd.app with version 0.1.0 identifier com.zensuite.zd sealed icon resource and valid signature plus a checksum-valid 4.2 MB zd_0.1.0_arm64.dmg
x 2026-08-05 (C) 2026-08-05 Publish version tags as GitHub Releases with downloadable macOS artifacts +p5 @package est:45m vis:9 DONE a v-prefixed semantic version tag now enters one GitHub-maintained release workflow whose first job rejects a tag that does not exactly match package.json and runs the existing static unit browser and native gates before artifacts can exist THE BUILD MATRIX uses GitHub's Apple Silicon and Intel macOS runners to invoke the same npm run package:macos command used locally then verifies every DMG and places its SHA-256 file beside it THE PUBLISH JOB alone receives contents write permission and creates a GitHub Release from the already-pushed tag after rechecking the collected checksums SUPPLY CHAIN SURFACE IS SMALL every action is GitHub-maintained and pinned to a full commit SHA RELEASE OPERATIONS are documented in docs/_internal/releasing.md including the honest v0.1 ad-hoc-signing and no-notarization boundary VERIFIED npm run check 358 passed npm run test:e2e 309 passed cargo test 38 passed strict cargo clippy cargo fmt workflow YAML parsing Prettier and npm run release:check -- v0.1.0
x 2026-08-05 (C) 2026-07-28 Install zd on PATH so the three launch forms work from a terminal +p5 @package sess:5.2 est:30m vis:9 DONE npm run install:macos now performs the documented conventional macOS install by replacing /Applications/zd.app as one complete bundle and linking /usr/local/bin/zd to its executable THE INSTALLER stages the new bundle before replacing the old one removes stale release contents updates the command through a stable absolute link and refuses to overwrite an unrelated zd command DESTINATIONS AND SOURCE are overridable so the real copy link and launch boundary is tested without privileges VERIFIED red first then 5 installer integration cases exercise bare zd zd md dot and zd md file from an outside working directory plus clean upgrades missing bundles and command collision refusal npm run check 363 passed 15 native CLI launch tests passed shell syntax and a copy of the real signed v0.1.0 app retained a valid signature and correct command link
x 2026-08-05 (C) 2026-08-05 Restructure public user and contributor documentation by purpose and shorten the README +p5 @docs est:60m vis:9 DONE the repository front page is now a 68-line product install quick-start status and routing page instead of carrying contributor automation internals THE DOCUMENTATION HUB routes readers by Diátaxis purpose into one learning path two operational guides one exact CLI reference and one architecture explanation while keeping design history audits task state and the reusable workflow visible as project records THE USER PAGES are honest about the current no-document surface macOS-first scope ad-hoc signing and missing notarization CONTRIBUTING now sends setup work to the development how-to VERIFIED red first then 10 information-architecture and local-link contracts npm run check 373 passed Prettier and diff whitespace checks
x 2026-08-05 (C) 2026-08-05 Create accurate v0.1.0 product and social media with reusable launch copy +p5 @package est:45m vis:9 DONE the README now shows a native 1100 by 760 capture from the built signed app with its real workspace rendered editable Markdown and paragraph focus state THE 1200 by 630 social card is deterministically rendered by CoreGraphics from that capture the shipped icon fonts and design palette with the UI crop removing only the computer-use pointer and titlebar A SINGLE PROMOTION KIT carries GitHub Release prose a 280-character social post exact asset uses install URL and image alt text while naming the Apple Silicon Intel ad-hoc-signing and no-notarization boundaries VERIFIED red first then exact JPEG and PNG dimension contracts README placement reusable render command copy sections local links npm run promo:render visual inspection and the full repository gate
