# Feedback inbox

Write what you noticed, one thought per line, below the rule. No format. Start a line with `!` if
it blocks you.

`/triage` turns these into tasks and moves your raw lines to `docs/_internal/objectives/feedback-archive.md`.
`/session` does the same before it picks up anything new.

Nothing above the rule is feedback.

---
