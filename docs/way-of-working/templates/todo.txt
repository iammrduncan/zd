# <project> — todo.txt
# Legend (these # lines are a legend, not tasks)
#   priority  (A) on the path to daily use  (B) after daily use  (C) before shipping
#   +p0..+p5  phase             @ctx  area of work
#   sess:N.N  session id from <plan>       est:  target duration
#   ref:<id>  a finding from the previous attempt this task fixes
#   +fb       came from FEEDBACK.md, the human inbox   +found  agent-findings.md, the agent queue
#   vis:N.N   section of <vision>
#   Rule: one session = one goal = one commit. Not done at 60m? Cut scope, commit what works.
#   Done lines start with `x <date>`. Open list: grep -v '^x ' todo.txt
#   Older done lines live in todo-archive.txt, moved there by /archive. Same format.
#   Drive it with /status, /session, /triage, /archive — see .claude/commands/
#   Full grammar: task-format.md, in the way-of-working folder
#   Drive a run with: /loop 60s /session until you reach the next checkpoint
#   The 60s is the gap after a session ends — room to read the handoff and stop the run

# --- phase 0: whatever has to be true before feature work can start ---
(A) 2026-01-01 EXAMPLE delete these three lines and write your own +p0 @setup sess:0.1 est:15m vis:1
(A) 2026-01-01 EXAMPLE one line per thing that is true when it is done +p0 @setup sess:0.1 est:30m vis:2
x 2026-01-02 (A) 2026-01-01 EXAMPLE a finished line keeps its whole line and gains a leading x and a date +p0 @setup sess:0.1 est:5m

# --- phase 1: the thing that solves the actual pain, and nothing else ---
(A) 2026-01-01 EXAMPLE the smallest version of the product you would use yourself +p1 @core sess:1.1 est:45m vis:4
(A) 2026-01-01 CHECKPOINT use it for a day and file feedback before phase 2 +p1 @reset est:0m

# --- phase 2 onward: what real use tells you it needs. Do not plan this far in detail yet. ---
(B) 2026-01-01 EXAMPLE later work sits here at (B) until daily use promotes or deletes it +p2 @core est:30m vis:5

(A) 2026-01-01 RECURRING triage FEEDBACK.md into tasks at the start of every session @reset est:5m
