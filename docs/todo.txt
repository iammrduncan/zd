# zd md second prototype — todo.txt
# Legend (these # lines are a legend, not tasks)
#   priority  (A) daily-driver path  (B) after daily use  (C) ship
#   +p0..+p5  phase             @ctx  area of work
#   sess:N.N  session id from docs/path-forward.md   est:  target duration
#   ref:F##   original finding from initial-prototype/feedback.md
#   +fb       came from FEEDBACK.md, the human inbox   +found  agent-findings.md, the agent queue
#   vis:N.N   section of docs/goals/second-prototype/vision.md
#   Rule: one session = one goal = one commit. Not done at 60m? Cut scope, commit what works.
#   Done lines start with `x <date>`. Open list: grep -v '^x ' todo.txt
#   Older done lines live in todo-archive.txt, moved there by /archive. Same format.
#   Drive it with /status, /session, /triage, /archive — see .claude/commands/
#   @DECIDE asks a question. @COMPARE renders options side by side so @DECIDE can be answered by
#   looking rather than reasoning — @COMPARE always comes first. Plain prose has no control effect.

x 2026-08-03 (A) 2026-08-03 Make zdloop decisions identify exactly what comparison to inspect without requiring terminal scrollback +p4 @reset +fb fb:2026-08-03 est:30m vis:12
x 2026-08-03 (B) 2026-08-03 Show zdloop model thinking level and Fast mode state at the top of every TUI screen +p4 @reset +fb fb:2026-08-03 est:20m vis:12

# --- phase 2 is the editor, re-sequenced 2026-07-29: one mode means the caret comes first ---
#
# Re-sequenced again 2026-07-29 by decision: appearance before behaviour. The bare CodeMirror
# surface reads as a code view, which is not the product — the editor has to be indistinguishable
# from the reader before anything is built on it. So decorations first, then save, then the flip.
# The flip lands after save and unsaved state on purpose: never ship a surface you can type into
# and cannot write out.
#
# Re-sequenced a third time 2026-07-29 by request: focus contrast first and iterate on it, then
# the commands needed to see what works, then everything that changes what the document LOOKS
# like, then everything that changes how it BEHAVES, and only then retiring the render path —
# which is blocked until the look tasks above it are done, so it now unblocks itself in sequence.


# --- how the document behaves ---
(A) 2026-08-05 Show a small reading progress indicator at the bottom right with distance through and remaining +p2 @reader +fb fb:2026-08-05 est:30m vis:4.1
x 2026-08-03 (B) 2026-08-03 Move the reading column left when word wrap is off so long lines use more of the viewport before overflowing +p2 @editor +fb fb:2026-08-03 est:30m ref:F03 vis:6.1
(B) 2026-07-30 A rendered link is not clickable +p2 @editor +fb fb:2026-07-30 est:30m vis:4.3 RELATED to the two phase 3 reader link tasks which cover in-window navigation and the browser handoff but neither covers the editor surface DECIDE whether a click in an editor activates or places a caret since both are what a click means here BLOCKED 2026-08-01 twice over first the DECIDE is the user's to make and second there is nothing to activate INTO 4.3 says a relative link navigates in the reader and only genuine http links cross to the system browser and BOTH of those are open +p3 tasks so making a link clickable today would wire a gesture to a hole ASK the DECIDE when phase 3 is closer or answer it with a COMPARE ANSWERED 2026-08-01 clicking link just places a caret at the link does not open it SO A CLICK ACTIVATES rather than places a caret which settles the first half of the DECIDE STILL BLOCKED on the second half since 4.3's relative-link navigation and the browser handoff are both open +p3 tasks and there is still nothing to activate INTO so this moves with them rather than before them RE-VERIFIED 2026-07-30 at the checkpoint rather than taken on trust and it is a STRONGER block than this line claimed NOTHING IN src CALLS openExternal at all so the browser handoff is not merely unfinished it is unbuilt and the mini app only ever reads ctx.launch.path with setText used by the reconcile path alone so there is no code path that opens a DIFFERENT document either A CLICK TODAY HAS LITERALLY NOWHERE TO GO NEW SINCE THE BLOCK WAS WRITTEN the filesystem scope shipped 2026-07-30 already permits a sibling markdown file since the launch folder is the root so when 4.3's relative-link navigation lands it will not need a second scope decision THIS IS THE LAST OPEN LINE IN THE BAND so the band is done-or-blocked and the checkpoint below is reached
x 2026-08-03 (B) 2026-08-03 Make the chrome-free window draggable from its top area +p2 @shell +fb fb:2026-08-03 est:20m vis:2

# --- now the look tasks above are done this unblocks itself ---

x 2026-08-03 (B) 2026-08-01 DECIDE whether the highlighting inventory widens past Rust or 6.2 stops asking for language-appropriate highlighting +p2 @design @DECIDE +found found:2026-08-01 est:20m vis:6.2 THE TWO SENTENCES CONTRADICT vision 6.2 says a non-markdown file gets language-appropriate highlighting and DESIGN.md 5.2 fixes the inventory at Rust alone and adds that an absent or unknown language remains honest monospace text rather than receiving misleading language colour BUILT 5.2 on 2026-08-01 because it is the narrower claim and because widening is not an implementation detail COST a ts or html file today is honest monospace with no colour which is defensible and is also not what 6.2 promises OPTIONS widen the inventory which means new grammars new colour categories beyond 5.2's three and a design system change or reword 6.2 to promise honest monospace outside the inventory NOTE the grammars are cheap lang-javascript covers ts jsx and tsx and lang-html brings css and js with it so this is a design question and not a packaging one SEE the closed non-markdown task above for what already works
x 2026-08-03 (B) 2026-08-03 COMPARE clamped notation markers and a wider compact inset at the same narrow viewport side by side +p4 @design @COMPARE est:20m vis:4.2
x 2026-08-03 (B) 2026-07-30 At a narrow window the notation markers hang past the left edge of the screen and cannot be reached +p4 @design @DECIDE +found found:2026-07-30 est:30m vis:4.2 MEASURED 2026-07-30 at a 600px window the leftmost glyph on the editing surface sits 16px PAST the window frame so a heading's hash is half cut off and there is no scroll position that shows it since a surface cannot scroll left of zero THE HANG IS DELIBERATE the markers live outside the column which is 7.3's the reading column stays a clean straight line and the space they hang in is the 5.3 inset AT THE WIDE VALUE 64px there is room AT THE COMPACT 32px and the 24px floor there is not and nothing checks that the hang fits the inset it depends on FOUND while measuring the trailing edge for the word wrap margin task which is why the leading control in tests/e2e/editor-trailing-inset.spec.ts measures the COLUMN's own box rather than the leftmost glyph DECIDE clamp the hang to the inset that is actually available or raise the compact inset to fit the widest marker since DESIGN.md 5.3 fixes both numbers so either way is a spec change ANSWERED 2026-08-03 use the Widen compact inset on the compare markers
x 2026-08-03 (B) 2026-08-03 COMPARE paragraph and section focus on the same one-H1 document side by side +p1 @reader @COMPARE est:20m vis:4.1
x 2026-08-03 (B) 2026-07-29 DECIDE section granularity is inert when a document has one H1 change 7.6 or leave it +p1 @reader @DECIDE +found found:2026-07-29 est:20m vis:4.1
x 2026-08-03 (B) 2026-08-03 COMPARE h3 to h6 with stepped space alone and with stepped type side by side +p2 @design @COMPARE est:30m vis:4.2
x 2026-08-03 (B) 2026-07-30 DECIDE whether h3 to h6 need bigger size steps as well as stepped space 5.2 fixes them a pixel apart +p2 @design @DECIDE +found found:2026-07-29 est:20m vis:4.2
x 2026-08-03 (B) 2026-08-03 COMPARE inline code in headings with Mono Regular and a real Mono Bold face side by side +p2 @design @COMPARE est:20m vis:4.2
x 2026-08-03 (B) 2026-07-30 DECIDE whether inline code in a heading reading lighter is acceptable or needs a Mono Bold face in the 5.1 manifest +p2 @design @DECIDE +found found:2026-07-28 est:20m vis:4.2 DEFERRED by request until after the phase 2 checkpoint
x 2026-08-03 (B) 2026-08-03 COMPARE title-only and section-spaced mid-document h1 treatment side by side +p2 @design @COMPARE est:20m vis:4.2
x 2026-08-04 (B) 2026-07-29 DECIDE whether a mid-document h1 sits in space reading.css gives it none because h1 is the title +p2 @design @DECIDE +found found:2026-07-29 est:20m vis:4.2
x 2026-08-04 (B) 2026-07-28 Suppress the focus colour transition while a theme or dim setting is being applied +p4 @shell +found found:2026-07-28 sess:4.4 est:20m vis:6.3
x 2026-08-04 (B) 2026-07-28 Measure the widest ordered marker per list instead of a fixed 1.9em column only if 100+ item lists matter +p4 @reader +found found:2026-07-28 est:45m vis:4.2
x 2026-08-04 (B) 2026-07-29 Line granularity in the reader paints a shaped row too getClientRects gives one rect per row +p4 @reader +found found:2026-07-29 est:30m ref:F04 vis:4.1
x 2026-08-04 (B) 2026-08-03 Stop zdloop asking again when an open DECIDE task already carries an ANSWERED verdict +p4 @reset +found found:2026-08-03 est:30m vis:12
x 2026-08-04 (B) 2026-08-03 Make the check harness accept the intentionally absent CI workflow +p4 @reset +found found:2026-08-03 est:20m vis:12 SEEN npm run check has six baseline failures because packages/scripts/tests/unit/ci-workflow.test.ts requires .github/workflows/check.yml but the workflow is absent
x 2026-08-04 (B) 2026-08-03 Align typography specs with the 22 / 20 / 18 / 16 deep-heading scale +p4 @design +found found:2026-08-03 est:20m vis:4.2 SEEN the typography specs still assert the old 20 / 19 / 18 / 17 scale so npm run test:e2e has three baseline failures even though the editor heading coverage expects the production scale
x 2026-08-04 (B) 2026-08-03 Launch each comparison before asking for its decision and remove its dev artifact after the answer +p4 @reset +fb fb:2026-08-03 est:45m vis:12
(B) 2026-08-03 Make `/` load the styled app in e2e runs instead of a blank default document +p4 @reset +found found:2026-08-03 est:45m vis:12 SEEN two full runs each finished 292 passed and 19 failed while every `/dev/` route passed BLOCKED 2026-08-04 the failure did not reproduce: the focused root-route files passed 22 of 22 and a full run loaded `/` successfully in every root test while finishing 304 passed and 5 unrelated editor failures; boot.spec.ts already asserts the md surface appears at `/` so there is no red regression to fix UNBLOCK with a failing root test or its error context from a full run
CHECKPOINT - stop and test

(B) 2026-07-29 Editor specs assert on the construct they mean identified by its text not on a count of everything that looks like it +p4 @editor +found found:2026-07-29 est:30m vis:12 SEEN adding a setext heading to the fixtures broke four specs two loudly and two quietly a second md-line-h1 made every bare locator waitFor a strict mode violation across ten files and the same for md-reading h1 WORSE headings survive being typed counted h1 globally and expected zero after re-levelling and focus-granularity asserts what a lone H1 does so a second one would have made it vacuous rather than failing NOTE the reader fixture now deliberately keeps exactly one H1 for that reason ALSO 2026-07-30 two assertions in editor-anchor.spec.ts went vacuous before landing both by using a proxy broader than the claim the target has no text also catches a horizontal rule which legitimately has none and failed the reader with nothing wrong then the target has child elements is true of every blank line because CodeMirror puts a br in each one so the second attempt passed on exactly the case it was written to catch RULE an assertion about a construct should name the construct and the closed list of legitimate exceptions belongs in the test ALSO 2026-08-01 a spec can cover the name of a claim and not the claim the target is the whole block not just the caret's line clicked into the MIDDLE of a paragraph and passed for four days while the paragraph's last position collapsed the target to one line RULE when a claim is about a range test its ENDS the middle is where everything works and a sweep over the whole range found the one bad position out of two hundred and eleven in a unit test that runs in a millisecond ALSO 2026-07-30 a distinct trap in the same family two of seven continuation specs passed before the feature existed because no stray marker was left behind is trivially true when nothing is being continued and one then failed on a slice reaching into the next fixture line which is allowed to contain anything RULE a claim about one line should read one line an absence asserted over a region is not red-first even when it is red ALSO 2026-07-30 two spec bugs in one session both from locating a line by its content startsWith triple-backtick-rust matched the fixture's existing rust fence two hundred lines above the new one and a caret placed on Ok(raw) was a line short of the block's last code line RULE locating by content is fine for reading and wrong for writing a spec that edits should anchor to a line number it computed not to a string that may appear more than once ALSO 2026-07-31 insertText and keyboard.type are not interchangeable insertText inserts a construct that never existed half typed so it hides every fault that depends on a construct completing under the caret which is exactly how the fence typing bug stayed invisible RULE anything about typing should type ALSO 2026-07-31 the table cell specs asserted a code element exists and that its text has no backticks both true and the feature was still broken because nothing styled it AN ELEMENT IS NOT A RENDERING RULE for anything in the renders list of 6.1 the claim is about what a reader sees so at least one assertion should be a computed style rather than a dom shape ALSO 2026-07-31 an exact number assertion about a position that legitimately moves is a guess dressed as a measurement word wrap's keeps the scroll position asserted scrollTop equals 600 and passed only because a bug scrolled the caret into view at coincidentally that number RULE a test that passes because of a bug fails when the bug is fixed which is the worst moment to find out so assert the intent where the caret's line sits on screen not the number ALSO 2026-07-31 a guard written next to a closed bug reads like the thing that proved it two specs added when the code block click report turned out already fixed do not detect it at all which was settled by reintroducing the suspected cause in one command RULE reintroduce the cause to learn whether a new spec is a regression test or merely coverage and say which in the file ALSO 2026-08-01 the same reintroduction showed a browser spec asserting something the browser could not decide the list indent multi item case drove its selection with Shift+ArrowDown which walks WRAPPED VISUAL ROWS not source lines so the selection did not end where the spec's line arithmetic said and the case stayed green with the bug it was written for reintroduced RULE if a claim is about a range of the document state it or the browser will choose the range for you and a keyboard-driven selection over wrapped text is not the range you wrote down SECOND RULE from the same commit a command that is a pure function of state and selection belongs in a unit test where the selection is offsets and the e2e file is left proving only what needs a browser which there was that the keys are bound at all ALSO 2026-08-01 a guard has to claim the key it guards onlyMoves first returned false when the wrapped command misbehaved on the reasoning that declining leaves the caret alone and lets other bindings try which is how continuation.ts is built and is right THERE but defaultKeymap binds the same chords so declining handed the press to the binding whose behaviour is the bug RULE declining is right when several commands want one key and each claims a different case claiming is right when the wrapper IS the binding both patterns now exist a file apart so each says which and why NOTE reading the failure would have beaten the two hypotheses I tried first the wrapper's own docstring described the layering it relied on and that layering was the bug
(B) 2026-07-30 The dev fixture keeps a second copy of the command list so every Shortcut Reference test measures the fixture rather than the product +p4 @editor +found found:2026-07-30 est:30m vis:7.1 SEEN index.ts registers the document's commands and editor/fixture.ts registers the same set again because the fixture builds the editor directly rather than mounting the mini app SO ADDING the focus block jump on 2026-07-30 meant writing the same two register calls twice and the count is now eight commands in two places NOTHING IS WRONG ON SCREEN TODAY and that is the problem every Reference spec navigates to the dev page so the suite proves the Reference renders A registry rather than THE PRODUCT'S and a command added to one and not the other passes every test and ships missing WHICH IS F16 with an extra step THE DRIFT 7.1 EXISTS TO PREVENT one level up since the registry solved it for bindings versus a hand written table and this is two registries FIX one exported registerDocumentCommands that both callers use SMALL BUT IT TOUCHES all eight registrations at once which is why it is a session rather than a corner of one SAME SHAPE as reader/fixture.ts rebuilding the reading column except that one duplicates markup nobody asserts against and this one duplicates the thing under test
(B) 2026-08-01 A scroll container's padding is a floor under its own size not only scroll extent teach it in way-of-working +p4 @reset +found found:2026-08-01 est:20m vis:12 SEEN box-sizing border-box with height 100 percent cannot produce a box smaller than its own padding so md-surface was 907px tall in a 700px window and silently ignored the height it was given CLASS any gutter big enough to matter belongs on the content because margin on the content is scroll extent too and cannot inflate the container NO SPEC CAUGHT IT for as long as every spec measured the surface's own box or its scrollTop the one that catches it now compares the container against the window which is the frame a reader actually has SYMPTOM was a caret leaving the screen and nothing about that pointed at padding
(B) 2026-08-01 When new behaviour needs to know what a construct is export the function that already answers +p4 @reset +found found:2026-08-01 est:20m vis:12 SEEN granularityOf was private in editor/focus.ts and exporting it took one line THREE of this week's defects were one construct described twice the list focus split the inline code rule and the anchor's five hand-written copies and each fix was to make one definition reachable rather than to write a second RULE a private helper is not a reason to form a second opinion and the block jump needed this before it had a chance to diverge
(B) 2026-07-30 CI is commented out by decision and the guards run on push again when the prototype is ready to be held to them +p4 @reset +fb fb:2026-07-30 est:5m vis:12 REPORTED as i commented out the ci we are not nearly ready for that yet DONE by commenting the whole of check.yml rather than deleting it so the workflow and the reasoning it carries stay in place AUDIT FINDING M5 which asked for it is still true and is deferred not answered NOTE the same file records that retries is deliberately 0 against one known intermittent spec which is most of why a red run today would not mean what it says UNBLOCKS ITSELF at the daily driver checkpoint when the suite is something a red result can be trusted against
(B) 2026-07-30 The created dates on task lines run two days ahead of the clock +p4 @reset @DECIDE +found found:2026-07-30 est:20m vis:12 SEEN lines created on 2026-07-30 carry 2026-08-01 in their date field and in their fb: and found: tags while every commit in the log is stamped 2026-07-30 and date +%F agrees with the commits SO THE FILE now holds lines that were completed on a date earlier than the one they were created on NOTHING IS LOST and no task is wrong the log simply cannot be read chronologically which is most of what a session log is for DECIDE correct the created dates in place or leave them and note the offset at the top of the file NOTE task-format.test.ts does not look at dates at all so neither the drift nor a correction is guarded and adding a rule that a completion date is not earlier than a creation date would catch the next one
(B) 2026-07-29 Editor specs never query a bare tag name inside the editor CodeMirror wraps every widget in its own img cm-widgetBuffer +p4 @editor +found found:2026-07-29 est:20m vis:12 SEEN four image specs failed with the implementation already correct because .md-editor img matched a zero size aria-hidden buffer thirteen img elements on the page and two are pictures a first waitFor on that selector waits forever for something that can never be visible and it presents as the feature not working FIXED in the image spec by going through .md-image the widget host WORTH a grep for any other spec querying a raw tag inside .md-editor
(B) 2026-07-29 Editor specs scroll until the wanted line is built instead of assuming a tall viewport rendered everything +p4 @editor +found found:2026-07-29 est:30m vis:12 SEEN broken three times as the fixture grew nested list ordered list links table each time unrelated specs went red and cost a diagnosis BUMPED to 4200 against a 3280px document which defers it NOTE scrollIntoViewIfNeeded cannot work on a line CodeMirror has not created so the helper has to scroll the surface and wait for the locator ALSO 2026-07-29 a second timing class in the same family lezer parses incrementally so a construct near the end of a long document is undecorated for a moment waiting on md-line-h1 says nothing about the last paragraph the four render specs now wait on their own construct in beforeEach and the helper should do both
(B) 2026-08-01 A spec that iterates the whole document has to justify iterating the whole document +p4 @editor +found found:2026-08-01 est:20m vis:12 CARRIED OUT of the suite task which is closed and was not about this in the end THE RULE IS STILL RIGHT wall time in a parallel run is not paid by the spec that spends it and a stride of seven covered the same constructs as all 115 lines when editor-caret was cut down SEEN editor-anchor was 6.3s alone editor-caret 3.6s focus-dim 3.9s NOTE the suite is 15.4 seconds green as of 2026-08-01 so this is prevention rather than repair and the honest form of it is a note in way-of-working plus a look at whether any sweep left is unjustified
(B) 2026-07-29 Stop the focus specs counting frames focus-motion samples one frame in focus-keyboard rides the two frame scroll guard and editor-focus-line races layout after ArrowDown +p4 @reader +found found:2026-07-29 est:45m vis:6.3 SEEN each fails only in full runs under parallel load and passes alone every time editor-focus-line failed 3 of 11 runs on 2026-07-29 DIAGNOSED 2026-07-29 the specs read a colour or a painted row while its 120ms transition is still running focus-dim asserted contrast greater than 3 and passed on 7 to 1 and 13 to 1 while the resting value was 3.5 to 1 the whole time REUSE the settledContext helper now at the top of focus-dim.spec.ts do not write a second one it needs a floor of frames before it trusts stability because a transition does not start until the next style recalculation TELL a mid-transition colour serialises as oklab instead of color srgb so that is greppable ONE OF THE THREE IS DONE 2026-08-01 editor-focus-line no longer races it polls on the claim through a clickAndSettle helper and waits for the prose face by name and it was not a flake at all it was consistently red in isolation see the closed suite task for the whole diagnosis LEFT focus-motion sampling one frame and focus-keyboard riding the two frame scroll guard both of which pass today PATTERN TO REUSE poll on the assertion's own subject rather than sleeping frames so a timeout fails for the same reason the assertion would CAUGHT ONE IN THE ACT 2026-08-01 focus-keyboard the surface can still scroll past the last target failed once in a full run and then passed 100 of 100 in isolation and three consecutive full runs after it so the rate is about one in four full runs and the diagnosis above still fits exactly NO PRODUCT CODE had changed that session only markdown THIS IS THE FIRST SIGHTING since editor-focus-line was fixed which means the remaining two are live rather than dormant and the suite is not unconditionally green PROMOTE IT when it costs a diagnosis rather than a re-run
(B) 2026-07-31 Fix the unit test localStorage harness so preferences tests run under npm test +p4 @reset +found found:2026-07-31 est:30m vis:12 SEEN all seven preferences tests consistently fail because window.localStorage has no clear getItem or setItem while the other 299 unit tests pass and both runs warn that --localstorage-file was provided without a valid path
CHECKPOINT - Before sidebar

(A) 2026-07-28 Sidebar file tree monospace collapsible and movable to either side +p3 @workspace sess:2.1 est:60m vis:5.1
(B) 2026-08-05 Draw a thin nesting guide from each expanded file-tree folder through its visible children +p3 @workspace +fb fb:2026-08-05 est:20m vis:5.1
(A) 2026-07-28 Markdown-only file filter with a toggle for everything else +p3 @workspace sess:2.1 est:20m vis:5.1
(A) 2026-07-28 Quick Open cmd+k with fuzzy ranking and keyboard navigation +p3 @workspace sess:2.2 est:45m vis:5.2
(A) 2026-07-28 Quick Open keeps one stable plane with no blank frame while typing +p3 @workspace sess:2.2 est:30m ref:F13 vis:5.2
(A) 2026-07-29 Relative workspace links navigate inside the reader +p3 @reader sess:2.3 +fb fb:2026-07-29 est:30m ref:F01 vis:4.3 SEEN raw markdown on white
(A) 2026-07-29 Only genuine http links open the system browser +p3 @reader sess:2.3 +fb fb:2026-07-29 est:15m ref:F01 vis:4.3 SEEN webview navigates with no way back
(A) 2026-07-28 Back and forward history for in-reader navigation +p3 @reader sess:2.3 est:30m vis:4.3
(A) 2026-07-28 Home screen with recent folders and files that persist +p3 @shell sess:2.4 est:45m vis:5.3
(A) 2026-07-28 Folder and file pickers plus create-file from Home +p3 @shell sess:2.4 est:30m vis:5.3
CHECKPOINT daily driver stop and live on it before phase 3 +p2 @reset est:0m


(B) 2026-07-28 Column splits via column-count with cmd+[ and cmd+] +p4 @reader sess:4.1 est:45m ref:F11 vis:4.4
(B) 2026-07-28 Column count reclamps on font size change and restores when it fits +p4 @reader sess:4.1 est:30m vis:4.4
(B) 2026-07-28 Shortcut Reference reads as a compact two-column markdown table +p4 @shell @DECIDE sess:4.3 est:30m ref:F15 vis:7.1 ALSO DECIDE the THREE chords this repo picked rather than looked up Mod-i for the status strip Mod-e for raw mode and Mod-Alt-t for typewriter added 2026-08-01 with 7.6 naming no key for it either DESIGN.md 7.10 gives the strip a command without naming a key and neither vision 6.1 nor 7.4 names one for raw mode both are registered and both appear in the Reference so nothing is hidden what is missing is agreement that they are the right keys and reading the whole list at once is the moment to settle it
(B) 2026-07-28 Settings surface for theme warmth dim granularity and sizes +p4 @shell sess:4.4 est:45m vis:7 NOTE 2026-08-01 focus granularity is the one row here that a shipped piece of feedback already depends on the paragraph-and-fence report is answered by section granularity and there is no way to select it outside the dev fixture hook so today the answer cannot be given to anyone
(B) 2026-07-29 Make the reading measure a setting DESIGN.md 5.3 fixes it and 7 does not list it +p4 @shell +fb fb:2026-07-29 sess:4.4 est:30m vis:7 MOVED 2026-07-30 out of the top of the file into this band since a task parked above the first CHECKPOINT gates that checkpoint on unrelated work
(B) 2026-07-28 Settings persist across restarts +p4 @shell @DECIDE sess:4.4 est:20m vis:7 STARTED 2026-08-01 suite/preferences.ts exists and holds word wrap with a named accessor per preference and localStorage behind it plus a memory fallback so a blocked webview still behaves REUSE IT do not write a second store the remaining settings are theme warmth dim granularity and sizes and each is one accessor pair DECIDE while doing it whether a file through platform.ts replaces localStorage which is a change to that one file
(B) 2026-08-03 Store review comments and shared app state in SQLite after the base md app is complete +p4 @shell +fb fb:2026-08-03 est:60m vis:7 DEFERRED by request until after the base md app build and should replace rather than sit beside the current localStorage-backed preference store
(B) 2026-07-28 Git status decoration in the sidebar with a toggle +p4 @workspace sess:4.5 est:45m vis:5.1
(B) 2026-08-03 Extract the file navigator into a reusable suite component after the base md app is complete +p4 @workspace +fb fb:2026-08-03 est:45m vis:3 DEFERRED by request until after the base md app build so multiple mini apps share one navigator rather than copying the md implementation
(B) 2026-07-28 Document outline reachable by keyboard that navigates on selection +p4 @reader sess:4.5 est:30m vis:4.3
(B) 2026-07-28 Find in document +p4 @reader sess:4.5 est:30m vis:4.3
(A) 2026-07-30 Table editing in rendered form cell contents add and remove rows and columns reorder by handle +p4 @editor +fb fb:2026-07-29 fb:2026-07-30 est:60m vis:4.2 PROMOTED 2026-07-30 by blocking feedback "table renders but is not editable unless going into raw mode" ALREADY COVERED this line rather than a second task NOTE the rendered table is a block widget so this and the caret tasks in phase 2 are the same subject seen from two ends REPORTED AGAIN 2026-08-01 tables are still not able to be edited in render mode which is the third time and it stays here rather than becoming a second line SEE ALSO the new blocking task about tables never dimming since both are the widget being outside what the surface does to everything else
(B) 2026-07-29 DECIDE whether Notion joins iA Writer and OmmWriter as a stated inspiration in vision 2 +p4 @design @DECIDE +fb fb:2026-07-29 est:20m vis:2 BLOCKS the table editing task above
CHECKPOINT - Stop and test

x 2026-08-05 (A) 2026-08-05 Audit and clean the repository for an open-source release +p5 @package est:60m vis:9
x 2026-08-05 (A) 2026-08-05 Back up and rewrite repository history into measurable public commits +p5 @package est:60m vis:9
x 2026-08-05 (A) 2026-08-05 Establish package.json as the 0.1.0 release version source +p5 @package est:60m vis:9
(C) 2026-07-28 Performance pass on a multi-megabyte agent log and virtualize if needed +p5 @perf sess:5.1 est:60m vis:10
(C) 2026-07-28 Measure and hold cold launch near 300ms to first meaningful frame +p5 @perf sess:5.1 est:30m vis:10
(C) 2026-07-28 Confirm idle CPU is near zero with a document open +p5 @perf sess:5.1 est:20m vis:10
(C) 2026-07-28 Multiple windows each with their own document and state +p5 @shell sess:5.2 est:45m vis:9
(B) 2026-07-29 Design our own app icon +p5 @package +fb fb:2026-07-29 est:45m vis:9 MOVED 2026-07-30 out of the top of the file to sit above the bundle task that consumes the icon
(C) 2026-07-28 macOS app bundle with icon and md file association +p5 @package sess:5.2 est:45m vis:9
(C) 2026-07-28 Install zd on PATH so the three launch forms work from a terminal +p5 @package sess:5.2 est:30m vis:9
(C) 2026-07-28 Windows build and short native checklist +p5 @package sess:5.3 est:60m vis:9

(C) 2026-07-29 Simple browser mini app for external links needs design thinking first +p5 @shell +fb fb:2026-07-29 est:60m vis:3

(A) 2026-07-28 RECURRING triage FEEDBACK.md into tasks at the start of every session @reset est:5m
