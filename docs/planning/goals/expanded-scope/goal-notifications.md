# Notifications md

start with threat notifications. When complete it pops up and shows desktop notification, clicking notification takes you directly to the thread.

notifications show "view" or "close" buttons on them.

notifications chime when triggered.

chimes can be configured differently per thread type (claude, codex, terminal, etc).
