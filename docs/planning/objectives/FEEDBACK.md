# Feedback inbox

Write what you noticed, one thought per line, below the rule. No format is required. Start a line
with `!` if it blocks you.

`$zd-triage` turns these notes into tasks and moves the raw lines to `feedback-archive.md`.
`$zd-session` does the same before selecting new work. Nothing above the rule is feedback.

---
