# Agent findings

This is the queue for real problems an agent notices outside its current task. Human observations
belong in `FEEDBACK.md`; agents never write there. `$zd-triage` processes human feedback first,
then turns these findings into scoped tasks.

---
