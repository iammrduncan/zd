# Rule: Work on each feedback item until resolved. Once resolved move it to below the Feedback Addressed Line. Mark it with "\" while feedback is in progress. One line per feedback item in this file.
# Goal command: `/goal address feedback in docs/planning/FEEDBACK.txt until complete.`
=== New Feedback ===
thread/project panel has weird bug around current file has unsaved work? ![Screenshot](../screenshots/screenshot-1787455459859109000.png)
files added to folder don't automatically show up in filetree. you can see in this screenshot I have pasted 2 screenshots but only one shows up on the file tree... ![Screenshot](../screenshots/screenshot-1787455512210124000.png). Only if I change the focus away and come back to the app does it show up.
cmd+j should really swap between the current thread/terminal and the current focused file. So if I swithc projects and come back I can get back to the already open file easily.
thread/project pane should be collapsable to icons only.
seems we need a general "context" setter function that sets the current view context based on all the variables here and whether it was a hotkey or clicking a thread or clicking a project etc... and it knows current threads contexts, worktrees, open files, etc and can restore those states etc... maybe?
=== Feedback Addressed ===
We should have project headers be darker background always so we can see at a glance what projects are open or not. ![Screenshot](../screenshots/screenshot-1787455389990233000.png)
we should have "THREADS" heading be "PROJECTS".
in code editor, currently selected line should be highlighted just like in zed and other editors.
cmd+. should open up hotkeys and keep it open. Hotkeys should be a nice condensed table not a spread apart list that makes it hard to see.
Should probably have a thin chrome at top of app to give some space between edge and other stuff since we have code now. Also gives us a good place to click and drige stuff around.
on hover of project header in thread shows "+" on right of it that allows you to start a new thread in that project.
remove the +- on the project header.
Right click project to get project context menue which includes "close"
hierarchy on the thread panel is funky with threads => + project => treads/projects => settings.
see reference thread screenshots of thread/project panes I like. Match these as closely as possible. ref1:docs/screenshots/zed-threads.png, ref2:docs/screenshots/workbench-1-x.png, ref3:docs/screenshots/herdr.png
"no file selected" should just be centered in the viewport.
thread pane and file/changes panes are not resizeable
I want to be able to paste a screenshot from clipboard inline to a file. When done it saves the screenshot to the project under a docs/screenshots/ directory and inserts link to the screenshot to the file. This will allow me to edit feedback txt or md files and paste screenshots inline to the feedback for you to inspect later.
terminal font is way too big, terminal font should be a monospaced font by default like fira code or ibm mono.
code editor that isn't markdown should be just like any ide with line numbers and syntax highlighting and open at top of the viewport etc...
