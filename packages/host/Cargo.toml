[package]
name = "zd-host"
version = "0.2.10"
description = "Host authority for the zd workbench"
edition = "2021"
license = "MIT"
repository = "https://github.com/iammrduncan/zd"

[dependencies]
ignore = "0.4"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
